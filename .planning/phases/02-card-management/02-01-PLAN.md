---
phase: 02-card-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/cardTemplates.ts
  - src/components/primitives/Modal.tsx
  - src/components/cards/IconRenderer.tsx
  - src/components/cards/CardVisual.tsx
  - src/sections/PaymentsSection.tsx
autonomous: true
requirements: [CARD-04, CARD-05, CARD-06]
user_setup: []

must_haves:
  truths:
    - "All cards render as credit-card-style visual tiles in a responsive grid (not a plain text list)"
    - "Each card tile shows masked number, use-case label, card-type chip (Physical/Virtual), and a status badge (Active/Frozen)"
    - "User can freeze an active card and unfreeze a frozen card; the badge updates and the change survives refresh"
    - "User can delete a card after confirming; it disappears from the grid and stays gone after refresh"
  artifacts:
    - path: "src/data/cardTemplates.ts"
      provides: "USE_CASE_TEMPLATES table (Groceries/SaaS/Travel/Shopping) + id/maskedNumber generators consumed by 02-02"
      contains: "USE_CASE_TEMPLATES"
    - path: "src/components/primitives/Modal.tsx"
      provides: "Reusable overlay+panel Modal used by 02-02 and 02-03"
      exports: ["Modal"]
    - path: "src/components/cards/IconRenderer.tsx"
      provides: "Maps a lucide icon name string (card.icon) to a rendered icon"
      exports: ["IconRenderer"]
    - path: "src/components/cards/CardVisual.tsx"
      provides: "Credit-card-style tile rendering a Card; reused by 02-02 preview and 02-03 detail"
      exports: ["CardVisual"]
      min_lines: 40
    - path: "src/sections/PaymentsSection.tsx"
      provides: "Responsive grid of CardVisual with per-card freeze/delete"
      contains: "CardVisual"
  key_links:
    - from: "src/sections/PaymentsSection.tsx"
      to: "useVaultStore.updateCard"
      via: "freeze/unfreeze handler sets status"
      pattern: "updateCard\\("
    - from: "src/sections/PaymentsSection.tsx"
      to: "useVaultStore.removeCard"
      via: "delete handler after confirm"
      pattern: "removeCard\\("
    - from: "src/sections/PaymentsSection.tsx"
      to: "src/components/cards/CardVisual.tsx"
      via: "import + map over cards"
      pattern: "CardVisual"
---

<objective>
Replace the plain text card list in PaymentsSection with a responsive grid of credit-card-style visual tiles, and add per-card freeze/unfreeze and delete. This plan also creates the shared building blocks the next two plans depend on: the use-case template table, a reusable Modal primitive, an IconRenderer, and the CardVisual component.

Purpose: Cards must read as real cards (CARD-04) and be manageable (CARD-05 freeze, CARD-06 delete). PaymentsSection is the shared file — this plan OWNS it; later plans only add modal mounts and click handlers to it.
Output: cardTemplates.ts, Modal.tsx, IconRenderer.tsx, CardVisual.tsx, reworked PaymentsSection.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png

<interfaces>
<!-- THE DATA CONTRACT — consume, do not change field names. From src/data/types.ts -->
```typescript
export type CardType = "physical" | "virtual";
export type CardStatus = "active" | "frozen";
export type UseCase = "groceries" | "saas" | "travel" | "shopping" | "general" | "custom";

export interface Card {
  id: string; type: CardType; useCase: UseCase; label: string;
  maskedNumber: string; status: CardStatus; limit: number; spent: number;
  mfaThreshold: number; mfaEnabled: boolean; color: string; icon: string;
  holder?: string; expiry?: string;
}
```

<!-- EXISTING STORE — USE THESE, do not recreate state. From src/store/useVaultStore.ts -->
```typescript
updateCard(id: string, partial: Partial<Card>): void;  // persists to localStorage
removeCard(id: string): void;                            // also removes the card's transactions; persists
addCard(card: Card): void;                                // persists
// Selector-per-field pattern: const cards = useVaultStore((s) => s.cards);
```

<!-- EXISTING ICON CONVENTION: card.icon is a lucide name string e.g. "ShoppingCart", "Plane", "CreditCard".
     Inline style is used for dynamic hex colors (card.color) because Tailwind JIT cannot do dynamic hex. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create cardTemplates.ts, Modal primitive, and IconRenderer</name>
  <files>src/data/cardTemplates.ts, src/components/primitives/Modal.tsx, src/components/cards/IconRenderer.tsx</files>
  <read_first>
    - src/data/types.ts (UseCase + Card contract)
    - src/components/primitives/Callout.tsx (existing rounded/pill aesthetic + X dismiss pattern to mirror)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (modal styling: white panel, rounded-2xl, soft shadow, X close top-left/right)
    - src/components/settings/SettingsModal.tsx (existing overlay: fixed inset-0 bg-black/40, rounded-2xl panel — match this overlay style)
  </read_first>
  <action>
    Create `src/data/cardTemplates.ts` exporting `USE_CASE_TEMPLATES`, a typed array consumed by plan 02-02. Use these EXACT values verbatim:

    ```typescript
    import type { UseCase } from "./types";
    export interface UseCaseTemplate {
      useCase: UseCase; label: string; icon: string; color: string; defaultLimit: number;
    }
    export const USE_CASE_TEMPLATES: UseCaseTemplate[] = [
      { useCase: "groceries", label: "Groceries", icon: "ShoppingCart", color: "#16A34A", defaultLimit: 800 },
      { useCase: "saas",      label: "SaaS",      icon: "Cloud",        color: "#4F46E5", defaultLimit: 300 },
      { useCase: "travel",    label: "Travel",    icon: "Plane",        color: "#0EA5E9", defaultLimit: 2000 },
      { useCase: "shopping",  label: "Shopping",  icon: "ShoppingBag",  color: "#DB2777", defaultLimit: 1000 },
    ];
    ```
    Also export two generator helpers (use Math.random — fine in app runtime):
    - `export function generateCardId(useCase: UseCase): string` → returns `` `card_${useCase}_${Math.random().toString(36).slice(2, 8)}` ``
    - `export function generateMaskedNumber(): string` → returns `` `•••• ${Math.floor(1000 + Math.random() * 9000)}` `` (a 4-digit group, always 4 digits).

    Create `src/components/primitives/Modal.tsx` exporting `Modal`. Props: `{ open: boolean; onClose: () => void; title: string; children: ReactNode; widthClass?: string }`. Behavior:
    - When `open` is false, render `null`.
    - Overlay: `fixed inset-0 bg-black/40 flex items-center justify-center z-[60]` (above the settings modal's z-50). Clicking the overlay (not the panel) calls `onClose`.
    - Panel: white, `rounded-2xl shadow-xl`, `widthClass ?? "w-[min(520px,calc(100vw-48px))]"`, `max-h-[calc(100vh-48px)] overflow-y-auto`, padding `p-6`. Stop click propagation on the panel.
    - Header row: `title` as `text-lg font-semibold text-gray-900` + an X close button (`lucide-react` `X`, `size={18}`, gray-400 hover gray-600) calling `onClose`, with `aria-label="Close"`.

    Create `src/components/cards/IconRenderer.tsx` exporting `IconRenderer`. Props: `{ name: string; size?: number; className?: string }`. Implementation: `import * as Icons from "lucide-react";` then look up `const Cmp = (Icons as Record<string, React.ComponentType<{ size?: number; className?: string }>>)[name] ?? Icons.CreditCard;` and render `<Cmp size={size ?? 18} className={className} />`. This maps the `card.icon` name string to a real icon.
  </action>
  <verify>
    <automated>grep -q "USE_CASE_TEMPLATES" src/data/cardTemplates.ts && grep -q "generateCardId" src/data/cardTemplates.ts && grep -q "generateMaskedNumber" src/data/cardTemplates.ts && grep -q "export function Modal" src/components/primitives/Modal.tsx && grep -q "export function IconRenderer" src/components/cards/IconRenderer.tsx && grep -q "4F46E5" src/data/cardTemplates.ts && npm run build</automated>
  </verify>
  <done>cardTemplates.ts exports USE_CASE_TEMPLATES (4 templates with the exact hex/limit values), generateCardId, generateMaskedNumber. Modal and IconRenderer exist and build passes (tsc + vite build exit 0).</done>
</task>

<task type="auto">
  <name>Task 2: Build CardVisual component</name>
  <files>src/components/cards/CardVisual.tsx</files>
  <read_first>
    - src/data/types.ts (Card contract — fields to render)
    - src/components/cards/IconRenderer.tsx (just created — use it for card.icon)
    - src/theme/tokens.ts (JUSPAY_ACCENT, JUSPAY_NAME)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (light/rounded aesthetic, pill chips, soft shadows)
    - src/sections/PaymentsSection.tsx (current swatch/badge classes to carry the visual language forward)
  </read_first>
  <action>
    Create `src/components/cards/CardVisual.tsx` exporting `CardVisual`. Props: `{ card: Card; onClick?: () => void; className?: string }`.

    Render a credit-card-style tile (aspect ratio ~1.6:1, `rounded-2xl`, `p-4`, `text-white`, `shadow-sm`, relative, `overflow-hidden`, cursor-pointer when onClick provided). Use a colored fill from `card.color` via inline style (Tailwind JIT cannot do dynamic hex — follow the existing inline-style convention): apply a subtle gradient `style={{ background: \`linear-gradient(135deg, ${card.color}, ${card.color}dd)\` }}`.

    Layout inside the tile:
    - Top row: left = `IconRenderer` with `name={card.icon}` (size 20, white) inside a small `bg-white/20 rounded-lg p-1.5` chip; right = a card-type chip showing "Physical" or "Virtual" derived from `card.type` (text-[10px] uppercase tracking-wide, `bg-white/20 rounded-full px-2 py-0.5`).
    - Middle: `card.label` (`text-sm font-semibold`) and below it `card.maskedNumber` (`text-base font-mono tracking-widest mt-1`).
    - Bottom row: left = status badge — if `card.status === "active"` show "Active" (`bg-white/25`), if "frozen" show "Frozen" (`bg-black/30`), both `text-[10px] rounded-full px-2 py-0.5`; right = a subtle Juspay mark: the `JUSPAY_NAME` text at `text-[10px] font-semibold text-white/80`.

    Keep it self-contained (no store calls — purely presentational; handlers come from the parent via onClick). If `onClick` is provided, wire it to the tile's root onClick and add `role="button"`.
  </action>
  <verify>
    <automated>grep -q "export function CardVisual" src/components/cards/CardVisual.tsx && grep -q "card.maskedNumber" src/components/cards/CardVisual.tsx && grep -q "card.label" src/components/cards/CardVisual.tsx && grep -Eq "Physical|Virtual" src/components/cards/CardVisual.tsx && grep -q "IconRenderer" src/components/cards/CardVisual.tsx && grep -q "card.color" src/components/cards/CardVisual.tsx && npm run build</automated>
  </verify>
  <done>CardVisual renders a colored card tile showing icon, label, masked number, Physical/Virtual chip, and Active/Frozen status badge. Uses inline style for card.color. Build passes.</done>
</task>

<task type="auto">
  <name>Task 3: Rework PaymentsSection into a card grid with freeze/unfreeze + delete</name>
  <files>src/sections/PaymentsSection.tsx</files>
  <read_first>
    - src/sections/PaymentsSection.tsx (current file being replaced)
    - src/components/cards/CardVisual.tsx (just built — render in the grid)
    - src/store/useVaultStore.ts (updateCard, removeCard — the actions to call)
    - src/components/branding/PoweredByJuspay.tsx (keep this mark)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (pill buttons, header text styles)
  </read_first>
  <action>
    Rewrite `PaymentsSection` keeping the existing header block ("Payments" h2 + "Manage cards your ChatGPT apps use." subtitle) and the existing "Reset demo" button + PoweredByJuspay footer.

    Replace the `<ul>` list with a responsive grid: `grid grid-cols-1 sm:grid-cols-2 gap-4`. Map over `cards` rendering each in a wrapper `<div>`. Inside each wrapper render `<CardVisual card={card} />` and BELOW it a small action row (`flex items-center gap-2 mt-2`):
    - Freeze/Unfreeze pill button: label is "Unfreeze" when `card.status === "frozen"` else "Freeze". onClick calls `updateCard(card.id, { status: card.status === "active" ? "frozen" : "active" })`. Style: `text-xs border border-gray-300 rounded-full px-3 py-1 text-gray-700 hover:bg-gray-50`.
    - Delete pill button: label "Delete", `text-xs border border-gray-300 rounded-full px-3 py-1 text-red-600 hover:bg-red-50`. onClick opens an inline confirm: track `confirmDeleteId` in `useState<string | null>(null)`; first click sets `confirmDeleteId = card.id`; while `confirmDeleteId === card.id`, render a confirm prompt ("Delete this card?" with "Confirm" → calls `removeCard(card.id)` then clears state, and "Cancel" → clears state) in place of / beside the action row. Confirm button red, Cancel gray pill.

    Add an "Add card" affordance at the top of the grid area (above or as a header-right action): a pill button `+ Add card` styled with JUSPAY_ACCENT background and white text (inline style `{ backgroundColor: JUSPAY_ACCENT }`, `rounded-full px-4 py-1.5 text-sm text-white`). For THIS plan its onClick may be a no-op placeholder `() => {}` — plan 02-02 wires it to the AddCardModal. Leave a clear `// wired in 02-02` comment so the next plan finds the hook.

    Get store actions via selector-per-field: `const updateCard = useVaultStore((s) => s.updateCard);` and `const removeCard = useVaultStore((s) => s.removeCard);`. Keep `const cards = useVaultStore((s) => s.cards);` and `const reset = useVaultStore((s) => s.reset);`.
  </action>
  <verify>
    <automated>grep -q "CardVisual" src/sections/PaymentsSection.tsx && grep -q "updateCard(" src/sections/PaymentsSection.tsx && grep -q "removeCard(" src/sections/PaymentsSection.tsx && grep -Eq "Freeze|Unfreeze" src/sections/PaymentsSection.tsx && grep -q "confirmDeleteId" src/sections/PaymentsSection.tsx && grep -q "Add card" src/sections/PaymentsSection.tsx && grep -Eq "grid-cols-2|sm:grid-cols-2" src/sections/PaymentsSection.tsx && npm run build</automated>
  </verify>
  <done>PaymentsSection shows cards in a responsive grid of CardVisual tiles; each has working Freeze/Unfreeze (calls updateCard with status) and Delete-with-confirm (calls removeCard); an "Add card" placeholder button exists; Reset demo + PoweredByJuspay remain. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (tsc + vite build).
- CardVisual, Modal, IconRenderer, cardTemplates exist and export their named symbols.
- PaymentsSection imports CardVisual and calls updateCard + removeCard.
- USE_CASE_TEMPLATES contains the exact 4 templates with hex #16A34A/#4F46E5/#0EA5E9/#DB2777 and limits 800/300/2000/1000.
</verification>

<success_criteria>
- All seeded cards render as card-style visual tiles in a responsive grid (CARD-04).
- Freeze/Unfreeze toggles a card's status via updateCard and persists (CARD-05).
- Delete removes a card via removeCard after a confirm step and persists (CARD-06).
- Shared building blocks (cardTemplates, Modal, IconRenderer, CardVisual) are exported for 02-02 and 02-03.
</success_criteria>

<output>
After completion, create `.planning/phases/02-card-management/02-01-SUMMARY.md`.
</output>
