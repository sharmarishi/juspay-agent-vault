---
phase: 02-card-management
plan: 03
type: execute
wave: 2
depends_on: ["02-01", "02-02"]
files_modified:
  - src/components/cards/CardDetailModal.tsx
  - src/sections/PaymentsSection.tsx
autonomous: true
requirements: [CARD-07]
user_setup: []

must_haves:
  truths:
    - "Clicking a card tile opens a detail view showing that card's large CardVisual"
    - "The detail view displays the card's current settings: limit, mfaThreshold, mfaEnabled, status (read-only this phase)"
    - "The detail view shows a spend-vs-limit indicator (spent / limit)"
    - "The detail view lists that card's recent transactions (date, merchant, amount, status), with subscriptions marked"
    - "The detail view offers freeze/unfreeze and delete actions for the card"
  artifacts:
    - path: "src/components/cards/CardDetailModal.tsx"
      provides: "Card detail modal: visual + read-only settings + spend bar + recent activity + freeze/delete"
      exports: ["CardDetailModal"]
      min_lines: 80
    - path: "src/sections/PaymentsSection.tsx"
      provides: "Tracks selected card and opens CardDetailModal on card click"
      contains: "CardDetailModal"
  key_links:
    - from: "src/components/cards/CardDetailModal.tsx"
      to: "useVaultStore.transactions"
      via: "filter transactions by cardId"
      pattern: "\\.cardId === "
    - from: "src/components/cards/CardDetailModal.tsx"
      to: "useVaultStore.updateCard / removeCard"
      via: "freeze and delete handlers"
      pattern: "updateCard\\(|removeCard\\("
    - from: "src/sections/PaymentsSection.tsx"
      to: "src/components/cards/CardDetailModal.tsx"
      via: "card onClick sets selected card and opens modal"
      pattern: "CardDetailModal"
---

<objective>
Add a card detail view: clicking a card opens a modal showing the large CardVisual, the card's current settings (limit, MFA threshold, MFA enabled, status — read-only this phase), a spend-vs-limit indicator, and a "Recent activity" list of that card's transactions. Includes freeze/unfreeze and delete from within the detail view.

Purpose: CARD-07. Settings are read-only here; Phase 3 makes limit/MFA editable and adds the simulate-payment flow. This plan delivers the detail shell.
Output: CardDetailModal.tsx + a wiring edit to PaymentsSection.tsx (card-click → open detail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png

<interfaces>
<!-- Contracts — from src/data/types.ts -->
```typescript
export interface Card { id; type; useCase; label; maskedNumber; status; limit; spent; mfaThreshold; mfaEnabled; color; icon; holder?; expiry?; }
export interface Transaction { id; cardId; appId; merchant; amount; date; status: "completed"|"pending"|"declined"; isSubscription: boolean; }
```

<!-- Built in 02-01 — REUSE. -->
```typescript
export function Modal(props: { open; onClose; title; children; widthClass? }): JSX.Element | null;
export function CardVisual(props: { card: Card; onClick?; className? }): JSX.Element;
export function IconRenderer(props: { name: string; size?; className? }): JSX.Element;
```

<!-- Store — USE THESE. From src/store/useVaultStore.ts -->
```typescript
// const transactions = useVaultStore((s) => s.transactions);
// const updateCard = useVaultStore((s) => s.updateCard);
// const removeCard = useVaultStore((s) => s.removeCard);
```

<!-- 02-02 added to PaymentsSection: const [addOpen,setAddOpen]=useState(false) and <AddCardModal .../>.
     This plan ADDS card-detail state alongside — do not remove the AddCardModal wiring. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CardDetailModal</name>
  <files>src/components/cards/CardDetailModal.tsx</files>
  <read_first>
    - src/data/types.ts (Card + Transaction contracts)
    - src/components/primitives/Modal.tsx (Modal wrapper)
    - src/components/cards/CardVisual.tsx (render the large visual)
    - src/store/useVaultStore.ts (transactions, updateCard, removeCard)
    - src/data/seed.ts (transaction shape + which cards have transactions, e.g. card_saas_01 has subscriptions)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (settings-row + progress aesthetic, pill buttons)
  </read_first>
  <action>
    Create `src/components/cards/CardDetailModal.tsx` exporting `CardDetailModal`. Props: `{ card: Card | null; onClose: () => void }`. Render `<Modal open={!!card} onClose={onClose} title={card?.label ?? "Card"} widthClass="w-[min(560px,calc(100vw-48px))]">`. If `card` is null, the Modal renders null (early-return guard `if (!card) return <Modal open={false} .../>` is fine, or guard the body).

    Get store: `const transactions = useVaultStore((s) => s.transactions);`, `const updateCard = useVaultStore((s) => s.updateCard);`, `const removeCard = useVaultStore((s) => s.removeCard);`.

    Body sections (top to bottom):
    1. Large `<CardVisual card={card} />` (no onClick).
    2. Spend-vs-limit indicator: a label row "Spent this period" with `$\{card.spent.toFixed(2)} / $\{card.limit}` and a progress bar — a `bg-gray-100 rounded-full h-2` track with an inner bar whose width is `${Math.min(100, (card.spent / card.limit) * 100)}%` (inline style width) and `backgroundColor: card.color` (inline). Guard divide-by-zero if limit is 0.
    3. Settings (READ-ONLY this phase): a small list of label/value rows — "Spending limit" → `$\{card.limit}`; "MFA threshold" → `$\{card.mfaThreshold}`; "MFA enforcement" → `card.mfaEnabled ? "On" : "Off"`; "Status" → `card.status === "active" ? "Active" : "Frozen"`. Add a tiny muted note: "Editable in a later step." (Phase 3 makes these editable — do NOT add inputs here.)
    4. "Recent activity": compute `const cardTxns = transactions.filter((t) => t.cardId === card.id).sort((a,b) => b.date.localeCompare(a.date));`. If empty, show "No activity yet." Otherwise render a list; each row: left = merchant (`text-sm`) + date (`text-xs text-gray-500`); if `t.isSubscription` show a small "Subscription" pill (`text-[10px] bg-gray-100 rounded-full px-2 py-0.5`); right = `$\{t.amount.toFixed(2)}` and a status chip colored by status (completed=green, pending=amber, declined=red — light bg pills).
    5. Action row at the bottom: Freeze/Unfreeze pill (label by `card.status`, onClick `updateCard(card.id, { status: card.status === "active" ? "frozen" : "active" })`) and Delete pill (red; onClick `removeCard(card.id)` then `onClose()`). Use the same pill styling as PaymentsSection (`text-xs border rounded-full px-3 py-1`).
  </action>
  <verify>
    <automated>grep -q "export function CardDetailModal" src/components/cards/CardDetailModal.tsx && grep -q "CardVisual" src/components/cards/CardDetailModal.tsx && grep -q ".cardId === " src/components/cards/CardDetailModal.tsx && grep -q "card.spent" src/components/cards/CardDetailModal.tsx && grep -q "card.mfaThreshold" src/components/cards/CardDetailModal.tsx && grep -q "isSubscription" src/components/cards/CardDetailModal.tsx && grep -Eq "updateCard\(|removeCard\(" src/components/cards/CardDetailModal.tsx && npm run build</automated>
  </verify>
  <done>CardDetailModal shows CardVisual, a spend-vs-limit bar, read-only limit/MFA/status settings, a filtered recent-activity list (subscriptions marked, status-colored), and freeze/delete actions. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire card-click to open CardDetailModal in PaymentsSection</name>
  <files>src/sections/PaymentsSection.tsx</files>
  <read_first>
    - src/sections/PaymentsSection.tsx (current — already mounts AddCardModal from 02-02; renders CardVisual in a grid)
    - src/components/cards/CardDetailModal.tsx (just built)
    - src/components/cards/CardVisual.tsx (accepts onClick prop)
  </read_first>
  <action>
    Edit `PaymentsSection`: add `const [detailCard, setDetailCard] = useState<Card | null>(null);` (import the `Card` type from `../data/types`). Pass `onClick={() => setDetailCard(card)}` to each `<CardVisual />` in the grid. Render `<CardDetailModal card={detailCard} onClose={() => setDetailCard(null)} />` at the end of the component (alongside AddCardModal). Do NOT remove the freeze/delete action row or the AddCardModal wiring — both must remain. Ensure clicking a card opens the detail and clicking the action-row pills (Freeze/Delete) does not also open the detail (the pills are below the CardVisual, not inside it, so they already don't trigger the card onClick — keep them outside the CardVisual element).
  </action>
  <verify>
    <automated>grep -q "CardDetailModal" src/sections/PaymentsSection.tsx && grep -q "setDetailCard" src/sections/PaymentsSection.tsx && grep -q "onClick={() => setDetailCard(card)}" src/sections/PaymentsSection.tsx && grep -q "AddCardModal" src/sections/PaymentsSection.tsx && npm run build</automated>
  </verify>
  <done>Clicking a card tile opens CardDetailModal for that card; AddCardModal and the freeze/delete action row still work. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- CardDetailModal filters transactions by cardId, shows read-only settings + spend bar + recent activity, and calls updateCard/removeCard.
- PaymentsSection opens CardDetailModal on card click while preserving AddCardModal and freeze/delete from earlier plans.
</verification>

<success_criteria>
- Clicking a card opens a detail view with its visual, settings (limit/MFA/status, read-only), spend-vs-limit indicator, and recent activity filtered to that card (CARD-07).
- Subscriptions are marked in the activity list.
- Freeze/unfreeze and delete are available from the detail view.
</success_criteria>

<output>
After completion, create `.planning/phases/02-card-management/02-03-SUMMARY.md`.
</output>
