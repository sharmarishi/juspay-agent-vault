---
phase: 03-controls-security-simulation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/cards/SimulatePaymentModal.tsx
  - src/components/cards/CardDetailModal.tsx
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03, SEC-04]

must_haves:
  truths:
    - "User can open a 'Simulate a payment' flow from a card and enter an amount + pick a connected app/merchant"
    - "A payment below the MFA threshold (or with MFA off) posts directly with no challenge"
    - "A payment above the MFA threshold (MFA on) shows a blocking OTP challenge; correct code posts it, cancel aborts it"
    - "A payment against a frozen card is rejected with the reason 'This card is frozen.'"
    - "A payment that would exceed the spending limit is rejected with 'This payment would exceed the card\\'s spending limit.'"
    - "A posted simulated payment increments card.spent and appears in recent activity / spend bar live"
  artifacts:
    - path: "src/components/cards/SimulatePaymentModal.tsx"
      provides: "Amount + app picker form, frozen/over-limit rejection, MFA OTP challenge step, posting via addTransaction + updateCard"
      contains: "DEMO_OTP"
      min_lines: 80
    - path: "src/components/cards/CardDetailModal.tsx"
      provides: "Simulate a payment button mounting SimulatePaymentModal"
      contains: "SimulatePaymentModal"
  key_links:
    - from: "src/components/cards/SimulatePaymentModal.tsx"
      to: "useVaultStore.addTransaction"
      via: "posting a completed transaction on success"
      pattern: "addTransaction\\("
    - from: "src/components/cards/SimulatePaymentModal.tsx"
      to: "useVaultStore.updateCard"
      via: "incrementing card.spent after posting"
      pattern: "updateCard\\([^,]+, \\{ spent:"
    - from: "src/components/cards/CardDetailModal.tsx"
      to: "src/components/cards/SimulatePaymentModal.tsx"
      via: "button opens the simulate modal"
      pattern: "<SimulatePaymentModal"
---

<objective>
Build the security demonstration: a "Simulate a payment" flow that enforces the per-card rules configured in Plan 03-01. Add a new SimulatePaymentModal (amount + app/merchant picker) and a button in CardDetailModal that opens it. The submit logic evaluates frozen → over-limit → MFA-challenge → direct-post, with verbatim rejection reasons and a blocking OTP challenge.

Purpose: This proves the Core Value — "trigger a simulated payment that is blocked by an MFA challenge when it exceeds the threshold."
Output: src/components/cards/SimulatePaymentModal.tsx (new) and a wired button in CardDetailModal.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/cards/CardDetailModal.tsx
@src/data/types.ts
@src/store/useVaultStore.ts
@src/components/primitives/Modal.tsx
@src/theme/tokens.ts

<interfaces>
<!-- Contracts the executor needs — use directly, no exploration. -->

Card (src/data/types.ts) relevant fields:
```typescript
id: string; status: "active" | "frozen";
limit: number; spent: number; mfaThreshold: number; mfaEnabled: boolean;
```

Transaction (src/data/types.ts) — exact shape to post:
```typescript
interface Transaction {
  id: string;          // unique
  cardId: string;      // = card.id
  appId: string;       // = chosen ConnectedApp.id
  merchant: string;    // = chosen ConnectedApp.name
  amount: number;
  date: string;        // ISO string
  status: "completed" | "pending" | "declined";
  isSubscription: boolean;
}
```

ConnectedApp (src/data/types.ts) — picker source from store `apps`:
```typescript
interface ConnectedApp { id: string; name: string; icon: string; }
```
Seeded apps available: app_instacart "Instacart", app_booking "Booking.com",
app_expedia "Expedia", app_spotify "Spotify", app_notion "Notion", app_chatgpt "ChatGPT".

Store actions (src/store/useVaultStore.ts) — REUSE, add no new state:
```typescript
addTransaction: (txn: Transaction) => void;       // persists
updateCard: (id: string, partial: Partial<Card>) => void; // persists
apps: ConnectedApp[];  // via useVaultStore((s) => s.apps)
cards: Card[];         // select live card by id for current spent/limit/status
```

Modal (src/components/primitives/Modal.tsx): `<Modal open onClose title widthClass>`.
Theme: `import { JUSPAY_ACCENT } from "../../theme/tokens"` → "#4F46E5".

Runtime note: `new Date().toISOString()` and `Math.random()` work in the app.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SimulatePaymentModal with form, rule evaluation, and OTP challenge</name>
  <files>src/components/cards/SimulatePaymentModal.tsx</files>
  <read_first>
    - src/data/types.ts (Transaction shape, Card fields, ConnectedApp)
    - src/store/useVaultStore.ts (addTransaction, updateCard, apps, cards)
    - src/components/primitives/Modal.tsx (wrapper props)
    - src/components/cards/CardDetailModal.tsx (aesthetic reference: rounded-xl, light borders, accent buttons)
    - src/theme/tokens.ts (JUSPAY_ACCENT)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (light/rounded/pill aesthetic; MFA "Secure your account" callout style for the challenge step)
  </read_first>
  <action>
    Create NEW file src/components/cards/SimulatePaymentModal.tsx.

    Imports: React (useState), Modal, useVaultStore, JUSPAY_ACCENT, types (Card).

    Module constant at top of file (exact):
    `const DEMO_OTP = "123456";`

    Props:
    ```typescript
    interface SimulatePaymentModalProps {
      cardId: string | null;   // null = closed
      onClose: () => void;
    }
    ```

    Store selectors inside the component:
    - `const cards = useVaultStore((s) => s.cards);`
    - `const apps = useVaultStore((s) => s.apps);`
    - `const addTransaction = useVaultStore((s) => s.addTransaction);`
    - `const updateCard = useVaultStore((s) => s.updateCard);`
    - Derive `const card = cards.find((c) => c.id === cardId) ?? null;`
    - If `!cardId || !card` → render `<Modal open={false} ...>{null}</Modal>` (mirror CardDetailModal's null guard).

    Local state:
    - `step: "form" | "challenge"` (useState, default "form")
    - `amount: string` (input value)
    - `appId: string` (selected app id; default to `apps[0]?.id ?? ""`)
    - `otp: string`
    - `error: string` (inline error / rejection reason; "" when none)

    Reset state when the modal opens for a card: use a `key={cardId}` on the rendered `<Modal>` is NOT enough for internal state; instead, the parent will remount via key (see Task 2), so default state is fine. Do not over-engineer.

    EVALUATION on form submit (a primary "Simulate payment" button) — evaluate in THIS EXACT ORDER and set `error` to the EXACT string, returning early on the first match:
    1. Parse `const amt = Number(amount);`. If not a finite number or `amt <= 0` → set error "Enter a valid amount." and return.
    2. If `card.status === "frozen"` → set error `"This card is frozen."` and return. (SEC-04)
    3. If `card.spent + amt > card.limit` → set error `"This payment would exceed the card's spending limit."` and return. (SEC-04)
    4. If `card.mfaEnabled && amt > card.mfaThreshold` → clear error, set `step = "challenge"` and return (do NOT post yet). (SEC-02)
    5. Otherwise → call `postPayment(amt)` directly (below). (payment below threshold / MFA off posts with no challenge)

    `postPayment(amt: number)` helper:
    - `const app = apps.find((a) => a.id === appId);`
    - Build the transaction with the EXACT shape:
      ```
      addTransaction({
        id: `txn_${Date.now()}_${Math.floor(Math.random() * 1000)}`,
        cardId: card.id,
        appId: appId,
        merchant: app?.name ?? "Unknown",
        amount: amt,
        date: new Date().toISOString(),
        status: "completed",
        isSubscription: false,
      });
      ```
    - Increment spent: `updateCard(card.id, { spent: card.spent + amt });`
    - Then `onClose();` (closing returns the user to CardDetailModal whose spend bar + recent activity now reflect the new txn live via the store).

    CHALLENGE step UI (when `step === "challenge"`) — SEC-03:
    - Render an MFA-styled callout block (light rounded card, like the "Secure your account" screenshot): a heading "Verify this payment", subtext explaining the amount exceeds the MFA threshold and requires approval.
    - A 6-digit OTP `<input>` bound to `otp` (inputMode numeric, maxLength 6).
    - A visible demo hint: text exactly containing `Enter the code: 123456` (e.g. `<p>Demo hint — Enter the code: {DEMO_OTP}</p>`).
    - Inline error area showing `error` when set.
    - "Verify & pay" primary button (style: backgroundColor JUSPAY_ACCENT, white text, rounded-full): on click, if `otp === DEMO_OTP` → call `postPayment(amt)` (recompute amt from the amount state via Number(amount)); else set error `"Incorrect code. Try again."` and stay on the challenge step.
    - "Cancel" button: on click, call `onClose()` and post NOTHING (abort). The challenge is blocking — there is no path that posts without correct OTP. (SEC-03)

    FORM step UI (when `step === "form"`) — SEC-01:
    - Amount: `<input type="number" min={0}>` with a "$" prefix, bound to `amount`.
    - App/merchant picker: a `<select>` bound to `appId`, options mapped from `apps` showing `app.name` as the label and `app.id` as value. The chosen app sets both appId and merchant (merchant = app.name resolved at post time).
    - Inline error area showing `error` when set (rejection reasons render here for frozen / over-limit / invalid amount).
    - Primary "Simulate payment" button (backgroundColor JUSPAY_ACCENT, white, rounded-full) → runs the evaluation above.

    Wrap everything in `<Modal open={true} onClose={onClose} title="Simulate a payment" widthClass="w-[min(440px,calc(100vw-48px))]">`.

    Match the light/rounded/pill aesthetic. Use JUSPAY_ACCENT for primary buttons only. Do NOT add new store actions or new global state.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/cards/SimulatePaymentModal.tsx exists and exports `SimulatePaymentModal`: `grep -q "export function SimulatePaymentModal" src/components/cards/SimulatePaymentModal.tsx`.
    - `grep -q 'const DEMO_OTP = "123456"' src/components/cards/SimulatePaymentModal.tsx` succeeds.
    - `grep -q "Enter the code: " src/components/cards/SimulatePaymentModal.tsx` succeeds (demo hint references DEMO_OTP).
    - `grep -q 'This card is frozen.' src/components/cards/SimulatePaymentModal.tsx` succeeds (SEC-04 frozen reason verbatim).
    - `grep -q "This payment would exceed the card's spending limit." src/components/cards/SimulatePaymentModal.tsx` succeeds (SEC-04 over-limit reason verbatim).
    - `grep -q "Incorrect code. Try again." src/components/cards/SimulatePaymentModal.tsx` succeeds (wrong-OTP inline error).
    - `grep -q "addTransaction(" src/components/cards/SimulatePaymentModal.tsx` succeeds and the call includes `status: "completed"` and `isSubscription: false` (grep both).
    - `grep -q "txn_\${Date.now()}" src/components/cards/SimulatePaymentModal.tsx` succeeds (unique txn id).
    - `grep -Eq "updateCard\(card.id, \{ spent:" src/components/cards/SimulatePaymentModal.tsx` succeeds (spent increment on post).
    - `grep -q 'step === "challenge"' src/components/cards/SimulatePaymentModal.tsx` succeeds (challenge step exists — SEC-02).
    - Evaluation order present: frozen check appears before the `card.spent + amt > card.limit` check, which appears before the `card.mfaEnabled && amt > card.mfaThreshold` check (verify by reading the submit handler).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    SimulatePaymentModal posts directly below threshold, shows a blocking OTP challenge above threshold (correct code posts, cancel aborts), and rejects frozen/over-limit payments with the verbatim reasons. Posting uses addTransaction (completed, non-subscription) and increments spent via updateCard. Build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "Simulate a payment" button in CardDetailModal that mounts SimulatePaymentModal</name>
  <files>src/components/cards/CardDetailModal.tsx</files>
  <read_first>
    - src/components/cards/CardDetailModal.tsx (modified by 03-01; this task only ADDS a button + modal mount, do not touch the Settings block from 03-01)
    - src/components/cards/SimulatePaymentModal.tsx (the component created in Task 1 — props: cardId, onClose)
    - src/components/primitives/Modal.tsx
    - src/theme/tokens.ts (JUSPAY_ACCENT)
  </read_first>
  <action>
    Edit src/components/cards/CardDetailModal.tsx (already reworked by Plan 03-01 with `liveCard`). Make ONLY these additive changes — do not modify the editable Settings block:

    1. Import the new component: `import { SimulatePaymentModal } from "./SimulatePaymentModal";`
    2. Add local state: `const [simulateOpen, setSimulateOpen] = useState(false);` (add `useState` to the React import if not already imported).
    3. Add a primary "Simulate a payment" button. Place it just above or beside the spend-vs-limit bar (a prominent spot), styled with JUSPAY_ACCENT background, white text, rounded-full, e.g. className `text-sm rounded-full px-4 py-1.5 text-white font-medium hover:opacity-90 transition-opacity` with `style={{ backgroundColor: JUSPAY_ACCENT }}`. onClick → `setSimulateOpen(true)`.
    4. Mount the modal near the end of the component's JSX (before `</Modal>` close, or just inside the root), keyed so internal state resets per open:
       `<SimulatePaymentModal cardId={simulateOpen ? liveCard.id : null} onClose={() => setSimulateOpen(false)} key={simulateOpen ? liveCard.id : "closed"} />`
       (Passing `cardId={null}` when closed keeps SimulatePaymentModal's Modal closed; the `key` toggling forces a fresh form/challenge state each time it opens.)
    5. Ensure JUSPAY_ACCENT is imported: `import { JUSPAY_ACCENT } from "../../theme/tokens";` (add if missing).

    Do NOT alter the editable limit/threshold/MFA controls from Plan 03-01. Do NOT add new store actions.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { SimulatePaymentModal }" src/components/cards/CardDetailModal.tsx` succeeds.
    - `grep -q "Simulate a payment" src/components/cards/CardDetailModal.tsx` succeeds (button label — SEC-01 entry point).
    - `grep -q "<SimulatePaymentModal" src/components/cards/CardDetailModal.tsx` succeeds (component mounted).
    - `grep -q "setSimulateOpen" src/components/cards/CardDetailModal.tsx` succeeds (open/close wiring).
    - `grep -q "JUSPAY_ACCENT" src/components/cards/CardDetailModal.tsx` succeeds (accent primary button).
    - The Settings edits from 03-01 are intact: `grep -q "updateCard(liveCard.id, { mfaEnabled:" src/components/cards/CardDetailModal.tsx` still succeeds.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    CardDetailModal has a JUSPAY_ACCENT "Simulate a payment" button that opens SimulatePaymentModal for the current card; closing it returns to the detail view whose spend bar and recent activity reflect any posted transaction. Plan 03-01's editable settings remain intact. Build passes.
  </done>
</task>

</tasks>

<verification>
- npm run build exits 0.
- SEC-01: "Simulate a payment" button opens a modal with amount input + app picker.
- SEC-02: amount > mfaThreshold with mfaEnabled → challenge step appears, no immediate post.
- SEC-03: correct DEMO_OTP posts via addTransaction; cancel aborts with no transaction.
- SEC-04: frozen card → "This card is frozen."; over-limit → "This payment would exceed the card's spending limit."
- Posting increments spent (updateCard) and the transaction is completed + non-subscription.
</verification>

<success_criteria>
- The full Core-Value flow works: configure (03-01) → simulate → MFA challenge above threshold → post or reject with reason.
- All four SEC requirements implemented using only existing store actions (addTransaction, updateCard).
- Posted payments appear live in the spend bar and recent activity (store-driven).
</success_criteria>

<output>
After completion, create `.planning/phases/03-controls-security-simulation/03-02-SUMMARY.md`
</output>
