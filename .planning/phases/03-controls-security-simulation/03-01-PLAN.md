---
phase: 03-controls-security-simulation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/cards/CardDetailModal.tsx
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04]

must_haves:
  truths:
    - "User can edit a card's spending limit in CardDetailModal and the new value persists"
    - "User can edit a card's MFA threshold in CardDetailModal and the new value persists"
    - "User can toggle MFA enforcement on/off per card in CardDetailModal"
    - "The spend-vs-limit progress bar reflects an edited limit live without closing the modal"
    - "The 'Editable in a later step.' note is gone"
  artifacts:
    - path: "src/components/cards/CardDetailModal.tsx"
      provides: "Editable spending limit + MFA threshold inputs and MFA enforcement Toggle wired to updateCard"
      contains: "updateCard"
  key_links:
    - from: "src/components/cards/CardDetailModal.tsx"
      to: "useVaultStore.updateCard"
      via: "onChange/onBlur of limit & threshold inputs, onChange of MFA Toggle"
      pattern: "updateCard\\(card\\.id, \\{ (limit|mfaThreshold|mfaEnabled)"
---

<objective>
Make the per-card Settings block in CardDetailModal editable: spending limit, MFA threshold, and MFA enforcement toggle. Replace the read-only display rows with live-editing controls wired to the existing `updateCard` store action.

Purpose: This is the configuration half of the Core Value — the user must be able to SET a limit and an MFA threshold (and turn MFA on/off) before the simulate-payment flow (Plan 03-02) can enforce them.
Output: An updated CardDetailModal.tsx whose Settings block has editable limit, editable MFA threshold, and an MFA on/off Toggle; the spend-vs-limit bar (CTRL-04) re-renders live as the limit changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/components/cards/CardDetailModal.tsx
@src/data/types.ts
@src/store/useVaultStore.ts
@src/components/primitives/Modal.tsx
@src/components/primitives/Toggle.tsx
@src/theme/tokens.ts

<interfaces>
<!-- Contracts the executor needs — use directly, no exploration. -->

Card (from src/data/types.ts) — relevant fields:
```typescript
status: "active" | "frozen";
limit: number;        // monthly spending limit USD
spent: number;        // spent this period
mfaThreshold: number; // amount above which MFA is required
mfaEnabled: boolean;  // MFA enforcement on/off
color: string;        // hex used on spend bar (inline style)
```

Store action (from src/store/useVaultStore.ts) — REUSE, do not add new state:
```typescript
updateCard: (id: string, partial: Partial<Card>) => void; // patches card, persists to localStorage
```

Toggle (from src/components/primitives/Toggle.tsx):
```typescript
<Toggle checked={boolean} onChange={(v: boolean) => void} />  // ON state uses JUSPAY_ACCENT
```

Theme: `import { JUSPAY_ACCENT } from "../../theme/tokens"` → "#4F46E5"
</interfaces>

NOTE: The component already reads `card` as a prop from PaymentsSection's `detailCard` state. After `updateCard`, the store changes but the `card` prop passed in is a stale snapshot. To make the spend bar and inputs reflect edits LIVE while the modal stays open, derive the current card from the store inside the component (select by id) instead of trusting the passed-in prop for display. See action.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make Settings block editable (limit, MFA threshold, MFA toggle) with live re-render</name>
  <files>src/components/cards/CardDetailModal.tsx</files>
  <read_first>
    - src/components/cards/CardDetailModal.tsx (the file being modified — Settings block is lines ~61-93)
    - src/data/types.ts (Card fields: limit, spent, mfaThreshold, mfaEnabled, status, color)
    - src/store/useVaultStore.ts (updateCard signature — the ONLY action used here)
    - src/components/primitives/Toggle.tsx (props: checked, onChange)
    - src/components/primitives/Modal.tsx (wrapper — do not change)
    - src/theme/tokens.ts (JUSPAY_ACCENT)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (light/rounded aesthetic; settings rows are label-left / control-right)
  </read_first>
  <action>
    Edit src/components/cards/CardDetailModal.tsx.

    1. LIVE STORE BINDING (so edits re-render without reopening): the component receives `card: Card | null` as a prop. Keep the early-return when prop is null. But for the live view, select the current card from the store by id:
       - Add `const cards = useVaultStore((s) => s.cards);` near the existing selectors.
       - After the null guard on the prop, compute: `const liveCard = cards.find((c) => c.id === card.id) ?? card;`
       - Use `liveCard` (NOT the prop) everywhere the body reads card fields: the CardVisual, the spend bar (`liveCard.spent`, `liveCard.limit`, `liveCard.color`), the Settings controls, recent-activity filter (`t.cardId === liveCard.id`), and the freeze/delete actions (use `liveCard.id`, `liveCard.status`). Recompute `spendPercent` from `liveCard`.
       - Keep `updateCard` and `removeCard` selectors as-is.

    2. REPLACE the read-only Settings block (the `{/* 3. Settings — READ-ONLY this phase */}` div, lines ~61-93) with editable controls. Keep the same outer container styling (border border-gray-100 rounded-xl, the gray header strip reading "Settings", divide-y rows). For each row keep label on the left, control on the right.

       Row A — Spending limit (CTRL-01):
       - A controlled number input. Use a local state value initialized from `liveCard.limit` so typing feels smooth, OR bind directly with onChange writing to store. Simplest acceptable: `<input type="number" min={0} defaultValue={liveCard.limit} ... />` is NOT acceptable because it won't reset on card switch. Instead use a controlled input keyed off `liveCard.id`: add `key={liveCard.id}` on the input so it re-inits when switching cards.
       - On change handler: parse the input value as a number; if it is a finite number >= 0, call `updateCard(liveCard.id, { limit: parsed })`. Ignore empty / NaN (do not write).
       - Prefix a "$" before the input. Style: small, right-aligned, rounded border (border border-gray-200 rounded-lg px-2 py-1 w-28 text-right text-sm), focus ring indigo.

       Row B — MFA threshold (CTRL-02):
       - Same pattern: `<input type="number" min={0}>` controlled, `key={liveCard.id}`, on valid number call `updateCard(liveCard.id, { mfaThreshold: parsed })`. Prefix "$". Add helper subtext under the label: "MFA required above this amount." (text-xs text-gray-400).

       Row C — MFA enforcement (CTRL-03):
       - Replace the "On/Off" text with the `Toggle` primitive: `<Toggle checked={liveCard.mfaEnabled} onChange={(v) => updateCard(liveCard.id, { mfaEnabled: v })} />`.

       Row D — Status:
       - Leave as a read-only display row (freeze/unfreeze stays in the existing Actions block at the bottom). Show "Active" / "Frozen" from `liveCard.status`.

    3. DELETE the footer note row containing the text "Editable in a later step." entirely.

    4. Do NOT add new store actions, new files, or a "Simulate a payment" button (that is Plan 03-02). Do NOT change Modal/Toggle/CardVisual.

    Keep all Tailwind classes consistent with the existing light/rounded aesthetic. Use JUSPAY_ACCENT only via the Toggle's built-in ON color (no new accent buttons in this plan).
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Editable in a later step" src/components/cards/CardDetailModal.tsx` returns 0.
    - `grep -q "updateCard(liveCard.id, { limit:" src/components/cards/CardDetailModal.tsx` succeeds (limit editing → updateCard).
    - `grep -q "updateCard(liveCard.id, { mfaThreshold:" src/components/cards/CardDetailModal.tsx` succeeds (threshold editing → updateCard).
    - `grep -q "updateCard(liveCard.id, { mfaEnabled:" src/components/cards/CardDetailModal.tsx` succeeds (MFA toggle → updateCard).
    - `grep -q "<Toggle" src/components/cards/CardDetailModal.tsx` succeeds (Toggle primitive reused for MFA enforcement).
    - `grep -q "const liveCard = cards.find" src/components/cards/CardDetailModal.tsx` succeeds (live store binding so spend bar reflects edits — CTRL-04).
    - `grep -q "MFA required above this amount" src/components/cards/CardDetailModal.tsx` succeeds (threshold helper text).
    - `grep -q "type=\"number\"" src/components/cards/CardDetailModal.tsx` succeeds (numeric inputs present).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>
    CardDetailModal Settings block has an editable spending-limit number input, an editable MFA-threshold number input, and an MFA-enforcement Toggle — all calling updateCard. The spend-vs-limit bar reads from the live store card so editing the limit updates the bar without reopening. The "Editable in a later step." note is removed. Build passes.
  </done>
</task>

</tasks>

<verification>
- npm run build exits 0.
- CTRL-01: spending-limit input calls updateCard with { limit }.
- CTRL-02: MFA-threshold input calls updateCard with { mfaThreshold }.
- CTRL-03: MFA Toggle calls updateCard with { mfaEnabled }.
- CTRL-04: spend bar derives from live store card (updates on limit edit).
</verification>

<success_criteria>
- All four CTRL requirements implemented via the existing updateCard action (no new state).
- Editing limit, threshold, or MFA toggle persists across refresh (updateCard already saves to localStorage).
- The spend-vs-limit progress bar reflects an edited limit live.
</success_criteria>

<output>
After completion, create `.planning/phases/03-controls-security-simulation/03-01-SUMMARY.md`
</output>
