---
phase: 04-transactions-usage-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sections/PaymentsSection.tsx
  - src/components/payments/DashboardOverview.tsx
  - src/components/payments/TransactionsList.tsx
autonomous: true
requirements: [DASH-01, DASH-02, TXN-02, TXN-03, TXN-04]
must_haves:
  truths:
    - "Payments section opens to an Overview dashboard by default"
    - "User can switch between Overview, Cards, and Transactions tabs"
    - "Overview shows total cards (active/frozen), total spent vs total limit, ~5 recent transactions, and MFA status across cards"
    - "Overview surfaces which cards have MFA disabled as a security nudge"
    - "Transactions tab lists ALL transactions across all cards, newest first, each showing merchant/app, owning card label, amount, date, status chip"
    - "Subscription transactions show a Subscription pill in the combined list"
    - "Simulated payments posted in Phase 3 appear in Overview recent list and Transactions list immediately (live store reads)"
    - "Cards tab preserves the existing grid with Add card, freeze, delete, card-click detail, and Reset demo all working"
  artifacts:
    - path: "src/components/payments/DashboardOverview.tsx"
      provides: "Overview dashboard stat tiles + recent transactions + MFA status, reads live from store"
      min_lines: 60
    - path: "src/components/payments/TransactionsList.tsx"
      provides: "Combined transactions list across all cards with subscription pill + status chip + card resolution"
      min_lines: 50
    - path: "src/sections/PaymentsSection.tsx"
      provides: "Tabbed Payments IA (overview/cards/transactions) preserving all card wiring"
      contains: "useState"
  key_links:
    - from: "src/sections/PaymentsSection.tsx"
      to: "useVaultStore"
      via: "s.cards selector for Cards tab grid"
      pattern: "useVaultStore\\(\\(s\\) => s\\.cards\\)"
    - from: "src/components/payments/DashboardOverview.tsx"
      to: "useVaultStore"
      via: "reads cards + transactions live"
      pattern: "useVaultStore"
    - from: "src/components/payments/TransactionsList.tsx"
      to: "useVaultStore"
      via: "reads transactions + cards live, resolves cardId->label"
      pattern: "useVaultStore"
---

<objective>
Restructure the Payments section from a flat card grid into a 3-tab layout (Overview, Cards, Transactions) and add two new read-only views that consume the store live.

Purpose: Deliver the at-a-glance payments dashboard (DASH-01, DASH-02) and the combined cross-card transaction history (TXN-02, TXN-03), with simulated Phase 3 payments appearing in both immediately (TXN-04). This is the demo's landing surface.
Output: Tabbed PaymentsSection.tsx, new DashboardOverview.tsx and TransactionsList.tsx components.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Use these directly — no codebase exploration needed. -->

From src/data/types.ts:
```typescript
interface Card {
  id: string; type: "physical" | "virtual"; useCase: UseCase;
  label: string; maskedNumber: string; status: "active" | "frozen";
  limit: number; spent: number; mfaThreshold: number; mfaEnabled: boolean;
  color: string; icon: string; holder?: string; expiry?: string;
}
interface Transaction {
  id: string; cardId: string; appId: string; merchant: string;
  amount: number; date: string; // ISO "YYYY-MM-DD"
  status: "completed" | "pending" | "declined"; isSubscription: boolean;
}
interface ConnectedApp { id: string; name: string; icon: string; }
```

From src/store/useVaultStore.ts (selector-per-field pattern — one selector per field):
```typescript
const cards = useVaultStore((s) => s.cards);              // Card[]
const transactions = useVaultStore((s) => s.transactions); // Transaction[]
const apps = useVaultStore((s) => s.apps);                 // ConnectedApp[]
const reset = useVaultStore((s) => s.reset);
const updateCard = useVaultStore((s) => s.updateCard);
const removeCard = useVaultStore((s) => s.removeCard);
```

From src/theme/tokens.ts:
```typescript
export const JUSPAY_ACCENT = "#4F46E5"; // use as inline style backgroundColor/color, NOT a Tailwind class (JIT can't handle dynamic hex)
```

Status chip color map already used in CardDetailModal (REUSE exactly):
```typescript
const statusColor = {
  completed: { bg: "bg-green-100", text: "text-green-700" },
  pending:   { bg: "bg-amber-100", text: "text-amber-700" },
  declined:  { bg: "bg-red-100",   text: "text-red-700" },
} as const;
```

Subscription pill (REUSE exactly): `<span className="text-[10px] bg-gray-100 rounded-full px-2 py-0.5 text-gray-500 shrink-0">Subscription</span>`
</interfaces>

@src/sections/PaymentsSection.tsx
@src/components/cards/CardDetailModal.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DashboardOverview component (DASH-01, DASH-02)</name>
  <files>src/components/payments/DashboardOverview.tsx</files>
  <read_first>
    - src/components/payments/DashboardOverview.tsx (creating — confirm absent first)
    - src/data/types.ts (Card, Transaction shapes)
    - src/store/useVaultStore.ts (selector-per-field pattern: read cards + transactions)
    - src/components/cards/CardDetailModal.tsx (REUSE statusColor map + Subscription pill markup + money formatting style)
    - src/theme/tokens.ts (JUSPAY_ACCENT inline-style usage)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (light/rounded aesthetic: rounded-xl tiles, gray-50 bg, gray-100 borders)
  </read_first>
  <action>
    Create `src/components/payments/DashboardOverview.tsx` exporting `export function DashboardOverview()`.
    Read live from store with separate selectors: `const cards = useVaultStore((s) => s.cards)` and `const transactions = useVaultStore((s) => s.transactions)`.

    Compute (all derived inline from the store arrays):
    - `totalCards = cards.length`, `activeCount = cards.filter(c => c.status === "active").length`, `frozenCount = cards.length - activeCount`.
    - `totalSpent = cards.reduce((s, c) => s + c.spent, 0)`, `totalLimit = cards.reduce((s, c) => s + c.limit, 0)`.
    - `mfaOnCount = cards.filter(c => c.mfaEnabled).length`; `mfaOffCards = cards.filter(c => !c.mfaEnabled)`.
    - `recent = [...transactions].sort((a, b) => b.date.localeCompare(a.date)).slice(0, 5)`.

    Render three stat tiles in a `grid grid-cols-1 sm:grid-cols-3 gap-3` row. Each tile: `rounded-xl border border-gray-100 bg-gray-50 px-4 py-3`, a small uppercase label (`text-xs font-semibold text-gray-500 uppercase tracking-wide`) and a large value (`text-xl font-semibold text-gray-900`):
      - Tile 1 "Cards": value `{totalCards}`, subline `text-xs text-gray-500` = `{activeCount} active · {frozenCount} frozen`.
      - Tile 2 "Total spent": value `${totalSpent.toLocaleString(undefined, { maximumFractionDigits: 0 })}`, subline = `of ${totalLimit.toLocaleString(undefined, { maximumFractionDigits: 0 })} limit`.
      - Tile 3 "MFA protection": value `{mfaOnCount}/{totalCards}`, subline = `cards with MFA on`. Color the value with `style={{ color: JUSPAY_ACCENT }}`.

    Below the tiles add an MFA nudge block (DASH-02): if `mfaOffCards.length > 0`, render a `rounded-xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm text-amber-800` block saying `MFA is off on {mfaOffCards.length} card{plural}:` followed by the card labels joined with `, ` (map mfaOffCards to `c.label`). If `mfaOffCards.length === 0`, render a `rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-800` block: `All cards have MFA enabled.`

    Below that add a "Recent transactions" section: heading `<p className="text-sm font-semibold text-gray-700 mb-2">Recent transactions</p>`. If `recent.length === 0` show `<p className="text-sm text-gray-400">No transactions yet.</p>`. Otherwise a `<ul>` with `divide-y divide-gray-100 border border-gray-100 rounded-xl overflow-hidden`; each `<li>` flex row (`px-4 py-2.5`) showing: left = merchant (`text-sm text-gray-900 truncate`) + Subscription pill when `t.isSubscription` (reuse exact pill markup) + date below (`text-xs text-gray-400`); right = amount `${t.amount.toFixed(2)}` + status chip using the REUSED `statusColor` map (same markup as CardDetailModal: `text-[10px] rounded-full px-2 py-0.5 font-medium ${statusColor[t.status].bg} ${statusColor[t.status].text}` with capitalized label).

    Use `import React from "react"` and import `useVaultStore` from `../../store/useVaultStore`, `JUSPAY_ACCENT` from `../../theme/tokens`. No props.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/payments/DashboardOverview.tsx exists and `grep -q "export function DashboardOverview" src/components/payments/DashboardOverview.tsx` succeeds.
    - `grep -q "useVaultStore((s) => s.cards)" src/components/payments/DashboardOverview.tsx` and `grep -q "useVaultStore((s) => s.transactions)" src/components/payments/DashboardOverview.tsx` both succeed (live store reads).
    - `grep -q "mfaEnabled" src/components/payments/DashboardOverview.tsx` succeeds (DASH-02 MFA status).
    - `grep -qE "reduce\(.*spent" src/components/payments/DashboardOverview.tsx` succeeds (total spent aggregation).
    - `grep -q "slice(0, 5)" src/components/payments/DashboardOverview.tsx` succeeds (recent ~5).
    - `grep -q "JUSPAY_ACCENT" src/components/payments/DashboardOverview.tsx` succeeds.
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>DashboardOverview renders stat tiles (cards count, total spent vs limit, MFA coverage), an MFA-off nudge, and a recent-transactions list — all computed live from the store. Type-checks clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build TransactionsList component (TXN-02, TXN-03, TXN-04)</name>
  <files>src/components/payments/TransactionsList.tsx</files>
  <read_first>
    - src/components/payments/TransactionsList.tsx (creating — confirm absent first)
    - src/data/types.ts (Transaction, Card, ConnectedApp)
    - src/store/useVaultStore.ts (read transactions + cards + apps)
    - src/components/cards/CardDetailModal.tsx (REUSE statusColor map, Subscription pill, row layout pattern for the activity list)
  </read_first>
  <action>
    Create `src/components/payments/TransactionsList.tsx` exporting `export function TransactionsList()`.
    Read live: `const transactions = useVaultStore((s) => s.transactions)`, `const cards = useVaultStore((s) => s.cards)`, `const apps = useVaultStore((s) => s.apps)`.

    Resolution helpers (inline finds over store arrays):
    - `const cardLabel = (cardId: string) => cards.find((c) => c.id === cardId)?.label ?? "Unknown card";`
    - `const appName = (appId: string) => apps.find((a) => a.id === appId)?.name ?? "";`

    Optional nice-to-have filter (include it): `const [filter, setFilter] = useState<"all" | "subscriptions">("all")`. Two pill buttons "All" / "Subscriptions" in a row above the list; active pill uses `style={{ backgroundColor: JUSPAY_ACCENT }}` + `text-white`, inactive uses `border border-gray-300 text-gray-600`. Both `text-xs rounded-full px-3 py-1`.

    Build the list: `const sorted = [...transactions].sort((a, b) => b.date.localeCompare(a.date));` then `const visible = filter === "subscriptions" ? sorted.filter((t) => t.isSubscription) : sorted;` (newest first across ALL cards — TXN-02).

    Render: if `visible.length === 0` → `<p className="text-sm text-gray-400">No transactions.</p>`. Otherwise a `<ul>` `divide-y divide-gray-100 border border-gray-100 rounded-xl overflow-hidden`. Each `<li key={t.id}>` flex row (`px-4 py-2.5 gap-2`):
      - Left (`min-w-0`): line 1 = `<span className="text-sm text-gray-900 truncate">{t.merchant}</span>` + Subscription pill when `t.isSubscription` (TXN-03, reuse exact markup). Line 2 (`text-xs text-gray-400 mt-0.5`) = `{appName(t.appId)} · {cardLabel(t.cardId)} · {t.date}` (this surfaces the owning card label + initiating app).
      - Right (`flex flex-col items-end gap-1 shrink-0`): amount `${t.amount.toFixed(2)}` (`text-sm font-medium text-gray-900`) + status chip via REUSED statusColor map (identical markup to CardDetailModal).

    Imports: `import React, { useState } from "react"`, `useVaultStore` from `../../store/useVaultStore`, `JUSPAY_ACCENT` from `../../theme/tokens`. No props. Because it reads `transactions` directly from the store, Phase 3 simulated payments (addTransaction) appear immediately (TXN-04) — no extra wiring needed.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/payments/TransactionsList.tsx exists and `grep -q "export function TransactionsList" src/components/payments/TransactionsList.tsx` succeeds.
    - `grep -q "useVaultStore((s) => s.transactions)" src/components/payments/TransactionsList.tsx` succeeds (live read → TXN-04).
    - `grep -qE "cards\.find\(\(c\) => c\.id === cardId\)" src/components/payments/TransactionsList.tsx` succeeds (cardId→label resolution).
    - `grep -qE "apps\.find\(\(a\) => a\.id === appId\)" src/components/payments/TransactionsList.tsx` succeeds (appId→name resolution / USAGE-02 attribution display).
    - `grep -q "Subscription" src/components/payments/TransactionsList.tsx` succeeds (TXN-03 pill).
    - `grep -q "localeCompare" src/components/payments/TransactionsList.tsx` succeeds (newest-first sort across all cards → TXN-02).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>TransactionsList renders every transaction across all cards newest-first with merchant, app+card label, amount, status chip, and a Subscription pill where applicable; an All/Subscriptions filter works; reads live so simulated payments appear instantly.</done>
</task>

<task type="auto">
  <name>Task 3: Restructure PaymentsSection into Overview/Cards/Transactions tabs</name>
  <files>src/sections/PaymentsSection.tsx</files>
  <read_first>
    - src/sections/PaymentsSection.tsx (the file being restructured — preserve ALL existing card wiring)
    - src/components/payments/DashboardOverview.tsx (created in Task 1 — to mount under Overview)
    - src/components/payments/TransactionsList.tsx (created in Task 2 — to mount under Transactions)
    - src/theme/tokens.ts (JUSPAY_ACCENT for active tab)
    - chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (tab styling must stay light/rounded)
  </read_first>
  <action>
    Edit `src/sections/PaymentsSection.tsx`. Add tab state: `const [tab, setTab] = useState<"overview" | "cards" | "transactions">("overview")` (default Overview so the dashboard is the landing view). Import `DashboardOverview` from `../components/payments/DashboardOverview` and `TransactionsList` from `../components/payments/TransactionsList`.

    Keep the existing header (`<h2>Payments</h2>` + subtitle) and the "+ Add card" button exactly as-is. The Add card button stays visible on all tabs (it sets `addOpen`).

    Add a tab bar directly below the header: a row (`flex items-center gap-1 border-b border-gray-100`) of three buttons. Each button `text-sm px-3 py-2 -mb-px border-b-2 transition-colors`; active tab uses `border-current font-medium` with `style={{ color: JUSPAY_ACCENT, borderColor: JUSPAY_ACCENT }}`, inactive uses `border-transparent text-gray-500 hover:text-gray-700`. Labels: "Overview" (sets tab "overview"), "Cards" (sets "cards"), "Transactions" (sets "transactions").

    Render content conditionally:
    - `tab === "overview"` → `<DashboardOverview />`.
    - `tab === "transactions"` → `<TransactionsList />`.
    - `tab === "cards"` → the EXISTING card grid block (the `cards.length === 0 ? ... : <div className="grid ...">...` mapping) PLUS the existing "Reset demo" block. Move these unchanged into the Cards tab. ALL existing wiring must be preserved verbatim: `CardVisual` with `onClick={() => setDetailCard(card)}`, the freeze/unfreeze button calling `updateCard`, the delete confirm flow using `confirmDeleteId`/`removeCard`, and the `reset` button.

    Keep `<PoweredByJuspay />`, `<AddCardModal .../>` and `<CardDetailModal .../>` mounted at the bottom of the component (outside the tab switch) exactly as before so detail/add modals work from the Cards tab. Do NOT remove any of the existing state hooks (`confirmDeleteId`, `addOpen`, `detailCard`) or store selectors (`cards`, `reset`, `updateCard`, `removeCard`).
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -qE 'useState<"overview" \| "cards" \| "transactions">\("overview"\)' src/sections/PaymentsSection.tsx` succeeds (tab union type, defaults to overview).
    - `grep -q "DashboardOverview" src/sections/PaymentsSection.tsx` and `grep -q "TransactionsList" src/sections/PaymentsSection.tsx` both succeed (both views mounted).
    - `grep -q "setDetailCard(card)" src/sections/PaymentsSection.tsx` succeeds (card-click detail wiring preserved).
    - `grep -q "confirmDeleteId" src/sections/PaymentsSection.tsx` and `grep -q "removeCard(card.id)" src/sections/PaymentsSection.tsx` succeed (delete flow preserved).
    - `grep -q "onClick={reset}" src/sections/PaymentsSection.tsx` succeeds (Reset demo preserved).
    - `grep -q "setAddOpen(true)" src/sections/PaymentsSection.tsx` and `grep -q "<AddCardModal" src/sections/PaymentsSection.tsx` and `grep -q "<CardDetailModal" src/sections/PaymentsSection.tsx` all succeed (add + modals preserved).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Payments opens to the Overview dashboard; tabs switch between Overview, Cards, and Transactions. The Cards tab retains the full grid with add/freeze/delete/detail/reset wiring intact. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0 (tsc + vite).
- Manual sanity (not blocking): open Payments → lands on Overview with stat tiles + MFA nudge + recent list; switch to Transactions → all txns newest-first with Subscription pills; switch to Cards → grid + Add card + freeze + delete + detail + Reset all work; trigger a Phase 3 Simulate-a-payment → new txn appears in Overview recent and Transactions list without refresh.
</verification>

<success_criteria>
- DASH-01: Overview summarizes all cards, total spend vs limits, and recent transactions. ✓
- DASH-02: Overview surfaces MFA status across cards (coverage count + which cards have MFA off). ✓
- TXN-02: Combined transaction history across all cards in the Transactions tab. ✓
- TXN-03: Subscriptions identifiable via Subscription pill in the combined list. ✓
- TXN-04: Views read live from the store; simulated payments appear immediately. ✓
- All existing Cards-tab wiring preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-transactions-usage-dashboard/04-01-SUMMARY.md`
</output>
