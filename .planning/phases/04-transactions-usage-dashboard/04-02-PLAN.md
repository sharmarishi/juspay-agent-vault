---
phase: 04-transactions-usage-dashboard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/cards/CardDetailModal.tsx
  - src/components/payments/AppUsageBreakdown.tsx
autonomous: true
requirements: [TXN-01, USAGE-01, USAGE-02]
must_haves:
  truths:
    - "Card detail shows which ChatGPT connected apps used the card and how much each spent"
    - "App usage is grouped by app, summed by amount, sorted by spend descending, with app name, icon, total, and transaction count"
    - "Per-card transaction history shows the full history for that card (not truncated), newest first"
    - "Each per-card transaction row is attributed to its initiating app/merchant"
    - "App usage updates live when a simulated payment posts to that card"
  artifacts:
    - path: "src/components/payments/AppUsageBreakdown.tsx"
      provides: "Per-card app spend breakdown: group by appId, sum amount, resolve appId->name/icon, sort desc"
      min_lines: 40
    - path: "src/components/cards/CardDetailModal.tsx"
      provides: "Detail modal enhanced with AppUsageBreakdown and full (untruncated) per-card history"
      contains: "AppUsageBreakdown"
  key_links:
    - from: "src/components/cards/CardDetailModal.tsx"
      to: "src/components/payments/AppUsageBreakdown.tsx"
      via: "renders AppUsageBreakdown with cardId prop"
      pattern: "<AppUsageBreakdown"
    - from: "src/components/payments/AppUsageBreakdown.tsx"
      to: "useVaultStore"
      via: "reads transactions + apps live, filters by cardId"
      pattern: "useVaultStore"
---

<objective>
Add a per-card "Used by apps" spend-attribution breakdown to the card detail view and ensure the per-card transaction history is complete (untruncated) with app attribution visible on each row.

Purpose: Deliver per-card app-usage attribution (USAGE-01), per-transaction app attribution (USAGE-02), and the full per-card transaction history (TXN-01). This is the differentiating agentic-commerce view: which connected app spent what on each card.
Output: New AppUsageBreakdown.tsx component + enhanced CardDetailModal.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Use these directly — no codebase exploration needed. -->

From src/data/types.ts:
```typescript
interface Transaction { id; cardId; appId; merchant; amount: number; date; status; isSubscription; }
interface ConnectedApp { id: string; name: string; icon: string; } // icon = lucide name e.g. "ShoppingCart"
```

From src/store/useVaultStore.ts (selector-per-field):
```typescript
const transactions = useVaultStore((s) => s.transactions);
const apps = useVaultStore((s) => s.apps);
```

IconRenderer (renders a lucide icon by string name; falls back to CreditCard):
```typescript
import { IconRenderer } from "../cards/IconRenderer";
<IconRenderer name={app.icon} size={18} className="text-gray-500" />
```

statusColor map + Subscription pill: see existing CardDetailModal (REUSE — already present in that file).
</interfaces>

@src/components/cards/CardDetailModal.tsx
@src/components/cards/IconRenderer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AppUsageBreakdown component (USAGE-01)</name>
  <files>src/components/payments/AppUsageBreakdown.tsx</files>
  <read_first>
    - src/components/payments/AppUsageBreakdown.tsx (creating — confirm absent first)
    - src/data/types.ts (Transaction, ConnectedApp)
    - src/store/useVaultStore.ts (read transactions + apps live)
    - src/components/cards/IconRenderer.tsx (icon rendering by lucide name + fallback)
    - src/components/cards/CardDetailModal.tsx (match list/row aesthetic: divide-y, border-gray-100, rounded-xl)
  </read_first>
  <action>
    Create `src/components/payments/AppUsageBreakdown.tsx` exporting `export function AppUsageBreakdown({ cardId }: { cardId: string })`.
    Read live: `const transactions = useVaultStore((s) => s.transactions)`, `const apps = useVaultStore((s) => s.apps)`.

    Aggregate this card's spend grouped by app:
    - Filter to card: `const cardTxns = transactions.filter((t) => t.cardId === cardId);`
    - Group-by-appId sum into a map: build `const totals = new Map<string, { total: number; count: number }>();` iterate cardTxns, for each accumulate `total += t.amount` and `count += 1` keyed by `t.appId`. (Include all statuses; this is usage attribution, not just settled spend.)
    - Resolve + sort: produce `const rows = Array.from(totals.entries()).map(([appId, agg]) => { const app = apps.find((a) => a.id === appId); return { appId, name: app?.name ?? "Unknown app", icon: app?.icon ?? "CreditCard", total: agg.total, count: agg.count }; }).sort((a, b) => b.total - a.total);` (sorted by spend descending — USAGE-01).

    Render: heading `<p className="text-sm font-semibold text-gray-700 mb-2">Used by apps</p>`. If `rows.length === 0` → `<p className="text-sm text-gray-400">No app usage yet.</p>`. Otherwise a `<ul>` `divide-y divide-gray-100 border border-gray-100 rounded-xl overflow-hidden`. Each `<li key={r.appId}>` flex row (`px-4 py-2.5 gap-2`):
      - Left: an icon chip (`bg-gray-100 rounded-lg p-1.5 shrink-0`) wrapping `<IconRenderer name={r.icon} size={18} className="text-gray-500" />`, then a column with app name (`text-sm text-gray-900`) and `text-xs text-gray-400` subline = `{r.count} transaction{r.count === 1 ? "" : "s"}`.
      - Right: total spent `${r.total.toFixed(2)}` (`text-sm font-medium text-gray-900`).

    Imports: `import React from "react"`, `useVaultStore` from `../../store/useVaultStore`, `IconRenderer` from `../cards/IconRenderer`. Reads from the store directly so simulated payments to this card update the breakdown live.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - File src/components/payments/AppUsageBreakdown.tsx exists and `grep -q "export function AppUsageBreakdown" src/components/payments/AppUsageBreakdown.tsx` succeeds.
    - `grep -q "{ cardId }" src/components/payments/AppUsageBreakdown.tsx` succeeds (accepts cardId prop).
    - `grep -qE "filter\(\(t\) => t\.cardId === cardId\)" src/components/payments/AppUsageBreakdown.tsx` succeeds (per-card filter).
    - `grep -qE "t\.appId" src/components/payments/AppUsageBreakdown.tsx` succeeds (group-by-appId).
    - `grep -qE "apps\.find\(\(a\) => a\.id === appId\)" src/components/payments/AppUsageBreakdown.tsx` succeeds (appId→app name/icon resolution).
    - `grep -qE "sort\(\(a, b\) => b\.total - a\.total\)" src/components/payments/AppUsageBreakdown.tsx` succeeds (sorted by spend desc).
    - `grep -q "IconRenderer" src/components/payments/AppUsageBreakdown.tsx` succeeds (app icon).
    - `grep -q "useVaultStore((s) => s.transactions)" src/components/payments/AppUsageBreakdown.tsx` succeeds (live read).
    - `npx tsc --noEmit` exits 0.
  </acceptance_criteria>
  <done>AppUsageBreakdown lists each app that spent on the given card with icon, name, transaction count, and total spent, sorted highest spend first, computed live from the store.</done>
</task>

<task type="auto">
  <name>Task 2: Wire AppUsageBreakdown into CardDetailModal + confirm full history (USAGE-01, USAGE-02, TXN-01)</name>
  <files>src/components/cards/CardDetailModal.tsx</files>
  <read_first>
    - src/components/cards/CardDetailModal.tsx (the file being edited — note existing cardTxns filter+sort and the "Recent activity" list)
    - src/components/payments/AppUsageBreakdown.tsx (created in Task 1)
    - src/data/types.ts (Transaction)
    - src/store/useVaultStore.ts (liveCard derivation pattern already used)
  </read_first>
  <action>
    Edit `src/components/cards/CardDetailModal.tsx`. Import `AppUsageBreakdown` from `../payments/AppUsageBreakdown`.

    1. (USAGE-01) Insert an `<AppUsageBreakdown cardId={liveCard.id} />` block ABOVE the existing "Recent activity" section (after the Settings block). Wrap it in a `<div className="mb-5">` to match surrounding spacing.

    2. (TXN-01 full history) The existing `cardTxns` already filters by `liveCard.id` and sorts newest-first via `localeCompare` — this is the FULL per-card history (no slice/truncation present). Confirm it is NOT sliced. Change the section heading from "Recent activity" to "Transaction history" so it reads as the complete per-card history. Leave the `.filter(...).sort(...)` logic intact (it must remain unsliced — full history).

    3. (USAGE-02) Each history row currently shows `t.merchant` + date. Add the initiating app name to the row subline so every transaction is attributed to its app. Add an inline resolver near the existing derivations: `const apps = useVaultStore((s) => s.apps);` and `const appName = (appId: string) => apps.find((a) => a.id === appId)?.name ?? "";`. In the row's date line, change `<p className="text-xs text-gray-400 mt-0.5">{t.date}</p>` to `<p className="text-xs text-gray-400 mt-0.5">{appName(t.appId)} · {t.date}</p>` so the app/merchant attribution is visible.

    Preserve everything else in the modal verbatim: the CardVisual, Simulate-a-payment button, spend bar, Settings block, freeze/delete actions, and SimulatePaymentModal mount. Do not remove the existing `transactions` selector or the statusColor map.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "import { AppUsageBreakdown }" src/components/cards/CardDetailModal.tsx` succeeds.
    - `grep -q "<AppUsageBreakdown cardId={liveCard.id}" src/components/cards/CardDetailModal.tsx` succeeds (wired with live card id → USAGE-01).
    - `grep -q "Transaction history" src/components/cards/CardDetailModal.tsx` succeeds (full-history heading; TXN-01).
    - `grep -qv "slice" src/components/cards/CardDetailModal.tsx` is NOT relied on; instead assert the cardTxns chain stays unsliced: `grep -q "cardTxns" src/components/cards/CardDetailModal.tsx` succeeds AND the cardTxns assignment line contains no `.slice(` — verify with `grep -n "cardTxns =" src/components/cards/CardDetailModal.tsx` then confirm that line and its filter/sort do not include `.slice(`.
    - `grep -qE "appName\(t\.appId\)" src/components/cards/CardDetailModal.tsx` succeeds (per-row app attribution → USAGE-02).
    - `grep -q "useVaultStore((s) => s.apps)" src/components/cards/CardDetailModal.tsx` succeeds (apps resolver added).
    - `npm run build` exits 0.
  </acceptance_criteria>
  <done>Card detail shows an "Used by apps" breakdown and a full "Transaction history" list where each row names the initiating app. Build passes.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0.
- Manual sanity (not blocking): open a card detail → "Used by apps" lists apps with per-app totals sorted by spend; "Transaction history" shows all of that card's transactions newest-first, each naming its app; run Simulate-a-payment on the card → the new transaction and its app's total update without refresh.
</verification>

<success_criteria>
- USAGE-01: Per-card view shows which connected apps used the card and how much each spent. ✓
- USAGE-02: Each transaction row is attributed to its initiating app. ✓
- TXN-01: Full per-card transaction history (date, app/merchant, amount, status; subscriptions identifiable via existing pill). ✓
</success_criteria>

<output>
After completion, create `.planning/phases/04-transactions-usage-dashboard/04-02-SUMMARY.md`
</output>
