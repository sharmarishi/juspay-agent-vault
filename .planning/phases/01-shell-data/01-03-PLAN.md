---
phase: 01-shell-data
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - src/sections/PaymentsSection.tsx
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "The Payments section lists the seeded cards read from the store (not hardcoded)"
    - "A 'Reset demo' button is visible in the Payments section"
    - "Clicking 'Reset demo' restores the seeded state via the store reset() and the change persists"
    - "Editing a card via the store and refreshing preserves the change (proven end-to-end through the shell)"
  artifacts:
    - path: "src/sections/PaymentsSection.tsx"
      provides: "Payments pane wired to useVaultStore: seeded card list + Reset demo button + Juspay co-brand"
      contains: "useVaultStore"
  key_links:
    - from: "src/sections/PaymentsSection.tsx"
      to: "src/store/useVaultStore.ts"
      via: "reads cards and calls reset()"
      pattern: "useVaultStore"
---

<objective>
Wire the Payments section (built as a static stub in 01-01) to the vault store
(built in 01-02): render the seeded cards from the store and add a visible
"Reset demo" button that calls the store's reset(). This proves the shell + nav +
data layer + persistence work end to end and satisfies DATA-03.

Purpose: Close the loop on Phase 1 — the foundation is real, not faked. Phase 2
will replace this minimal list with the full card-management UI.
Output: A functional Payments pane reading live store state with a working reset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-shell-data/01-01-SUMMARY.md
@.planning/phases/01-shell-data/01-02-SUMMARY.md

<interfaces>
From src/store/useVaultStore.ts (built in plan 01-02):
- useVaultStore() exposes: cards, transactions, apps, updateCard(id, partial), addCard(card), removeCard(id), addTransaction(txn), reset().

From src/data/types.ts:
- Card has: id, type, useCase, label, maskedNumber, status, limit, spent, mfaThreshold, mfaEnabled, color, icon (and holder/expiry on physical).

From src/components/branding/PoweredByJuspay.tsx (built in plan 01-01):
- default export <PoweredByJuspay/> renders the subtle co-brand mark.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire Payments pane to the store — seeded card list + Reset demo button</name>
  <read_first>
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/sections/PaymentsSection.tsx (current static stub)
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/store/useVaultStore.ts (store API)
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/data/types.ts (Card shape)
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/components/branding/PoweredByJuspay.tsx
  </read_first>
  <files>src/sections/PaymentsSection.tsx</files>
  <action>
    Replace the static stub body in PaymentsSection.tsx with a store-driven minimal Payments pane (this is intentionally minimal — full card UI is Phase 2):
    - Import and call useVaultStore to read `cards` and the `reset` action.
    - Heading "Payments" (h2, text-xl font-semibold) with a one-line subtitle e.g. "Manage cards your ChatGPT apps use."
    - Render the cards as a simple vertical list. Each row: a small color swatch (div with inline backgroundColor: card.color), the card.label, the card.maskedNumber (text-gray-500), and a status badge (active = green-ish pill, frozen = grey pill). Use card.id as key. Map over the store cards — do NOT hardcode card data.
    - Empty state: if cards.length === 0, show "No cards yet." muted.
    - A "Reset demo" button (bordered rounded-full pill, text-sm) at the bottom of the section that calls reset() on click. Place a short helper line near it: "Restore the original seeded demo data."
    - Keep the <PoweredByJuspay/> mark at the bottom of the section.
    Do not modify any other file. (Persistence + reset logic already lives in the store from 01-02.)
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/sections/PaymentsSection.tsx contains "useVaultStore"
    - src/sections/PaymentsSection.tsx contains "reset" and "Reset demo"
    - src/sections/PaymentsSection.tsx contains "maskedNumber"
    - src/sections/PaymentsSection.tsx contains "PoweredByJuspay"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Payments pane lists seeded cards from the store and a working Reset demo button; build passes.</done>
</task>

</tasks>

<verification>
- `npm run dev`: clicking Payments shows the seeded cards (multiple cards, masked numbers, status badges) read from the store.
- The "Reset demo" button is visible and clicking it restores seeded state.
- Manual end-to-end: mutate a card (e.g. via the store/devtools), refresh — change persists; click Reset demo — seeded state restored and persists across refresh.
- `npm run build` exits 0.
</verification>

<success_criteria>
- DATA-03: a Reset demo action is surfaced in the UI and restores the seeded state ✓
- End-to-end proof: shell (01-01) + data layer (01-02) + persistence are connected and working ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-data/01-03-SUMMARY.md` noting the Payments pane is now store-driven, the Reset demo affordance location, and that Phase 2 replaces this minimal list with full card management.
</output>
