---
phase: 01-shell-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/types.ts
  - src/data/seed.ts
  - src/store/useVaultStore.ts
  - src/store/persistence.ts
autonomous: true
requirements: [DATA-01, DATA-02]

must_haves:
  truths:
    - "The app boots with realistic seeded cards, transactions, subscriptions, and per-app usage available in a store"
    - "Card changes saved through the store persist to localStorage and survive a refresh"
    - "Cards span physical and virtual, multiple use-cases (Groceries, SaaS, Travel, Shopping), each with masked number, status, limit, mfaThreshold, mfaEnabled, color, icon"
    - "Transactions include recurring subscriptions and are attributed to a ChatGPT app/merchant"
    - "The store exposes a reset() that restores the original seeded state"
  artifacts:
    - path: "src/data/types.ts"
      provides: "TypeScript domain model for cards, transactions, apps"
      contains: "mfaThreshold"
    - path: "src/data/seed.ts"
      provides: "Realistic seed dataset (cards + transactions + connected apps)"
      contains: "Groceries"
    - path: "src/store/persistence.ts"
      provides: "load-from-localStorage-or-seed + save helpers"
      contains: "localStorage"
    - path: "src/store/useVaultStore.ts"
      provides: "App-wide store hook with cards/transactions/apps + actions incl. reset"
      contains: "reset"
  key_links:
    - from: "src/store/useVaultStore.ts"
      to: "src/store/persistence.ts"
      via: "store initializes from loadState() and calls saveState() on mutation"
      pattern: "saveState|loadState"
    - from: "src/store/persistence.ts"
      to: "src/data/seed.ts"
      via: "falls back to SEED when localStorage is empty"
      pattern: "SEED|seed"
---

<objective>
Define the data model and ship the seeded, persistent data layer the whole demo
runs on: TypeScript types for cards/transactions/connected-apps, a realistic seed
dataset, a localStorage persistence layer (load-or-seed + save), and a lightweight
app-wide store (Zustand) exposing cards/transactions/apps plus actions including a
reset() that restores the seeded state.

Purpose: Later phases (card management, controls, simulate-payment, dashboard)
read and write cards & transactions through this store. It must be correct and
complete now so those phases only add behavior, not plumbing.
Output: `useVaultStore` hook + seed + persistence, all type-checked.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

<data_model_contract>
Define these exact shapes in src/data/types.ts (field names are the contract for downstream phases):

CardType = "physical" | "virtual"
CardStatus = "active" | "frozen"
UseCase = "groceries" | "saas" | "travel" | "shopping" | "general" | "custom"

interface Card {
  id: string;                // e.g. "card_groceries_01"
  type: CardType;
  useCase: UseCase;
  label: string;             // e.g. "Groceries", "Netflix & SaaS"
  maskedNumber: string;      // e.g. "•••• 4821"
  status: CardStatus;        // active | frozen
  limit: number;             // monthly spending limit in USD
  spent: number;             // spent this period (for spend-vs-limit bar later)
  mfaThreshold: number;      // amount above which MFA is required
  mfaEnabled: boolean;       // MFA enforcement on/off
  color: string;             // hex used on the card visual
  icon: string;              // lucide icon name (string) e.g. "ShoppingCart"
  holder?: string;           // for physical cards
  expiry?: string;           // "MM/YY" for physical cards
}

interface Transaction {
  id: string;
  cardId: string;            // FK -> Card.id
  appId: string;             // FK -> ConnectedApp.id (the initiating ChatGPT app/merchant)
  merchant: string;          // display merchant name
  amount: number;            // USD
  date: string;              // ISO date string
  status: "completed" | "pending" | "declined";
  isSubscription: boolean;   // recurring subscription flag
}

interface ConnectedApp {
  id: string;                // e.g. "app_instacart"
  name: string;              // "Instacart"
  icon: string;              // lucide icon name or emoji
}

interface VaultState {
  cards: Card[];
  transactions: Transaction[];
  apps: ConnectedApp[];
}
</data_model_contract>

<seed_contract>
Seed must be realistic and exercise every dimension. Minimums:
- >=5 cards: at least 1 physical (with holder "Rishi Sharma" + expiry + masked number) and >=4 virtual across distinct use-cases groceries/saas/travel/shopping. Vary status (include at least one frozen), distinct color hexes and icon names, sensible limit + mfaThreshold + mfaEnabled per card, and a non-zero spent value.
- >=5 ConnectedApps: e.g. Instacart, Booking, Expedia, Spotify, a SaaS tool (e.g. "Notion"/"Figma"). 
- >=10 Transactions across cards, attributed to apps, with realistic merchants, dates in the last ~60 days, mixed statuses, and at least 2 with isSubscription true (e.g. Spotify, a SaaS subscription).
Export const SEED: VaultState.
</seed_contract>

<state_approach>
Use Zustand (add "zustand" to dependencies). Concrete and lightweight per locked decision. Store exposes state (cards, transactions, apps) and actions. Persistence is manual (load on init, save on each mutation) so the schema is explicit and inspectable — do NOT use a heavyweight persist middleware config; keep it readable.
</state_approach>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Domain types + persistence layer (load-or-seed, save)</name>
  <read_first>
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/.planning/REQUIREMENTS.md (DATA-01/02/03)
    - The <data_model_contract> and <seed_contract> blocks in this plan
  </read_first>
  <files>src/data/types.ts, src/data/seed.ts, src/store/persistence.ts</files>
  <behavior>
    - loadState(): returns parsed VaultState from localStorage key "juspay-agent-vault" when present and valid JSON
    - loadState(): returns a deep clone of SEED when localStorage is empty
    - loadState(): returns a deep clone of SEED when localStorage value is malformed (try/catch)
    - saveState(state): writes JSON.stringify(state) to the same key; a subsequent loadState() returns the saved state
  </behavior>
  <action>
    Add "zustand" to package.json dependencies and run npm install.
    src/data/types.ts: declare exactly the types from <data_model_contract> (export all of them).
    src/data/seed.ts: import the types; export const SEED: VaultState satisfying <seed_contract>. Use deterministic ids (card_*, txn_*, app_*). Spread realistic values.
    src/store/persistence.ts:
      - export const STORAGE_KEY = "juspay-agent-vault";
      - export function loadState(): VaultState — read localStorage[STORAGE_KEY]; if missing/invalid (wrap JSON.parse in try/catch), return structuredClone(SEED). On success return the parsed VaultState.
      - export function saveState(state: VaultState): void — localStorage.setItem(STORAGE_KEY, JSON.stringify(state)).
      - Guard for SSR/no-window with typeof window checks (defensive) but assume browser.
    If a test runner is not yet configured, add vitest + jsdom as devDeps and a "test" script ("vitest run"), and a vitest.config.ts with environment "jsdom". Write src/store/persistence.test.ts covering the four behaviors above (mock/clear localStorage between cases).
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm install && npm test</automated>
  </verify>
  <acceptance_criteria>
    - src/data/types.ts contains "mfaThreshold" and "isSubscription"
    - src/data/seed.ts contains "Groceries" and at least one occurrence of "isSubscription: true"
    - src/store/persistence.ts contains "localStorage" and "STORAGE_KEY"
    - `npm test` exits 0 (persistence tests pass)
  </acceptance_criteria>
  <done>Types compile; seed satisfies the seed_contract; persistence loads-or-seeds and round-trips saves, proven by passing tests.</done>
</task>

<task type="auto">
  <name>Task 2: Zustand vault store with seeded init, persistence, and reset</name>
  <read_first>
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/data/types.ts
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/store/persistence.ts
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/data/seed.ts
  </read_first>
  <files>src/store/useVaultStore.ts</files>
  <action>
    Create the app-wide store with Zustand:
    - Initialize state from loadState() (so it boots seeded on first run, or from saved data after).
    - State: cards, transactions, apps (typed from VaultState).
    - Actions (each mutates then calls saveState with the new VaultState snapshot):
      * updateCard(id, partial: Partial<Card>) — patches a card by id.
      * addCard(card: Card) — appends a card.
      * removeCard(id) — removes a card and its transactions.
      * addTransaction(txn: Transaction) — appends a transaction.
      * reset() — sets state to structuredClone(SEED) AND calls saveState(SEED clone) so the persisted copy is also reset. This is the DATA-03 reset action surfaced in the UI in plan 01-03.
    - Export the hook as useVaultStore. Keep a small helper to build the VaultState snapshot for saveState from get().
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/store/useVaultStore.ts contains "useVaultStore" and "reset"
    - src/store/useVaultStore.ts contains "loadState" and "saveState"
    - src/store/useVaultStore.ts contains "updateCard"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>useVaultStore boots from seed/localStorage, mutations persist, and reset() restores + persists the seeded state.</done>
</task>

</tasks>

<verification>
- `npm test` passes (persistence behaviors).
- `npm run build` exits 0 (types + store compile).
- Store exposes cards/transactions/apps and updateCard/addCard/removeCard/addTransaction/reset.
- Seed contains >=5 cards across use-cases, a physical card, a frozen card, >=10 transactions, >=2 subscriptions.
</verification>

<success_criteria>
- DATA-01: realistic seeded cards, transactions, subscriptions, and app-usage data ✓
- DATA-02: changes persist across refresh via localStorage ✓
- (DATA-03 reset action is implemented in the store here and surfaced in the UI in plan 01-03)
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-data/01-02-SUMMARY.md` documenting the type shapes (field names downstream phases depend on), the seed contents summary, the store hook name + action signatures, and the localStorage key.
</output>
