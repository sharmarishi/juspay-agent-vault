---
phase: 01-shell-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - tsconfig.json
  - tsconfig.node.json
  - tailwind.config.js
  - postcss.config.js
  - index.html
  - src/main.tsx
  - src/index.css
  - src/App.tsx
  - src/theme/tokens.ts
  - src/components/settings/SettingsModal.tsx
  - src/components/settings/SettingsNav.tsx
  - src/components/settings/navItems.ts
  - src/components/settings/SettingsContent.tsx
  - src/components/primitives/SettingRow.tsx
  - src/components/primitives/Toggle.tsx
  - src/components/primitives/Dropdown.tsx
  - src/components/primitives/Callout.tsx
  - src/components/branding/PoweredByJuspay.tsx
  - src/sections/GeneralSection.tsx
  - src/sections/PlaceholderSection.tsx
  - src/sections/PaymentsSection.tsx
autonomous: true
requirements: [SHELL-01, SHELL-02, SHELL-03, SHELL-04]

must_haves:
  truths:
    - "Opening the app shows a ChatGPT-style settings modal with a left nav and a content pane in a clean light theme"
    - "The left nav lists all ChatGPT sections plus a new 'Payments' entry"
    - "Clicking a nav item swaps the content pane to that section; General shows screenshot-like rows, others show stub placeholders"
    - "Clicking Payments shows the Payments section with a 'Powered by Juspay' mark"
    - "An X (close) button is present in the modal header"
    - "The layout is usable at desktop widths (>=1024px)"
  artifacts:
    - path: "src/components/settings/SettingsModal.tsx"
      provides: "Modal overlay + frame holding nav and content; selected-section state"
      min_lines: 30
    - path: "src/components/settings/navItems.ts"
      provides: "Ordered nav item list incl. Payments"
      contains: "Payments"
    - path: "src/theme/tokens.ts"
      provides: "Design tokens incl. Juspay accent hex"
      contains: "JUSPAY_ACCENT"
    - path: "src/components/primitives/Toggle.tsx"
      provides: "Reusable on/off switch primitive"
    - path: "src/components/primitives/Dropdown.tsx"
      provides: "Reusable label+chevron dropdown control primitive"
    - path: "src/sections/PaymentsSection.tsx"
      provides: "Payments pane (static stub in this plan; wired to store in 01-03)"
  key_links:
    - from: "src/components/settings/SettingsNav.tsx"
      to: "src/components/settings/SettingsContent.tsx"
      via: "selected section id state lifted in SettingsModal"
      pattern: "selected"
    - from: "src/components/settings/SettingsContent.tsx"
      to: "src/sections/PaymentsSection.tsx"
      via: "renders PaymentsSection when selected === 'payments'"
      pattern: "payments"
---

<objective>
Scaffold the Vite + React + TypeScript + Tailwind project and build the ChatGPT
settings modal shell: overlay + frame, a left nav listing every ChatGPT section
plus a new "Payments" entry, a content pane that swaps by selected nav item, a
close (X) button, reusable row/toggle/dropdown/callout primitives, and the subtle
"Powered by Juspay" co-brand with a defined Juspay accent token.

Purpose: This is the foundation surface every later phase builds on. It must look
and feel like the reference ChatGPT Settings modal (light theme, rounded cards,
label-left/control-right rows, blue toggles, chevron dropdowns).
Output: A running SPA (`npm run dev`) that opens straight into the settings modal.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Visual source of truth — match these exactly
@chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png
@chatgpt screenshots/Screenshot 2026-06-09 at 11.33.20 AM.png
@chatgpt screenshots/Screenshot 2026-06-09 at 11.33.24 AM.png

<visual_contract>
Light theme only (Phase 1). Match the Settings-modal screenshot:
- Modal: white panel, ~rounded-2xl corners, soft shadow, on a translucent dark overlay (bg-black/40). Two columns: left nav ~260px fixed width, content pane fills the rest. Header row has an X close button top-left of the nav column and the active section title at the top of the content pane.
- Left nav: vertical list of icon+label rows. Active item has a light grey rounded-lg background (bg-gray-100), inactive items transparent with hover:bg-gray-50. Icon ~18px, label text-sm, text-gray-800. Padding ~px-3 py-2, rounded-lg, gap-3 between icon and label.
- Content rows (SettingRow): a horizontal flex row, label on the left (text-sm font-medium text-gray-900, optional sub-label text-xs text-gray-500 underneath), control on the right. Separated by subtle bottom borders (border-b border-gray-100), py-4.
- Toggle: pill switch ~w-11 h-6, rounded-full. OFF = bg-gray-300, ON = bg-[Juspay accent] (use accent for ON to subtly co-brand), white knob that translates on toggle.
- Dropdown: a pill button with label text + a chevron-down icon (lucide ChevronDown), text-sm text-gray-700, no real menu needed (static).
- Callout (MFA card): light grey rounded-xl card (bg-gray-50), an icon (shield), bold title, muted description, and an action button (the "Set up MFA" pill — bordered, rounded-full). Include a small X in its top-right.

Juspay accent color: use indigo "#4F46E5" exported as JUSPAY_ACCENT. This is the subtle co-brand accent (used for ON toggles and the Powered-by-Juspay mark). Keep everything else neutral grey/white per ChatGPT.

Nav item list (verbatim order, Payments inserted after Apps):
General, Notifications, Personalization, Apps, Payments, Data controls, Storage, Security, Parental controls, Trusted contact, Account, Keyboard.
Use lucide-react icons (any sensible match): Settings, Bell, UserCircle, Grid3x3, CreditCard (Payments), Database, HardDrive, ShieldCheck, Users, Contact, User, Keyboard.
</visual_contract>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TS + Tailwind with design tokens</name>
  <read_first>
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/CLAUDE.md (conventions/workflow)
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/.planning/PROJECT.md (locked tech stack)
  </read_first>
  <files>package.json, vite.config.ts, tsconfig.json, tsconfig.node.json, tailwind.config.js, postcss.config.js, index.html, src/main.tsx, src/index.css, src/App.tsx, src/theme/tokens.ts</files>
  <action>
    Initialize a Vite React-TS SPA at the repo root (do NOT create a nested subfolder — files live directly under the existing repo root).
    - package.json: name "juspay-agent-vault", scripts dev/build/preview using vite (build = "tsc && vite build"). deps: react, react-dom, lucide-react. devDeps: vite, @vitejs/plugin-react, typescript, tailwindcss, postcss, autoprefixer, @types/react, @types/react-dom. Use current stable versions.
    - vite.config.ts: import react plugin, export default defineConfig({ plugins: [react()] }).
    - tsconfig.json: strict true, jsx "react-jsx", moduleResolution "bundler", target ES2020, lib ["ES2020","DOM","DOM.Iterable"], include ["src"]. Add tsconfig.node.json referencing vite.config.ts.
    - tailwind.config.js: content ["./index.html","./src/**/*.{ts,tsx}"]. Under theme.extend.colors add juspay: "#4F46E5". Nothing else exotic.
    - postcss.config.js: tailwindcss + autoprefixer plugins.
    - src/index.css: the three @tailwind directives (base, components, utilities); set body to bg-gray-100 antialiased text-gray-900.
    - index.html: root div#root, title "Juspay Agent Vault", load /src/main.tsx as module.
    - src/main.tsx: ReactDOM.createRoot rendering <App/> in StrictMode, import "./index.css".
    - src/App.tsx: render the SettingsModal (created in Task 2) full-screen as the entry point. For this task, a temporary placeholder <div>App</div> is acceptable; Task 2 replaces it.
    - src/theme/tokens.ts: export const JUSPAY_ACCENT = "#4F46E5"; export const JUSPAY_NAME = "Juspay"; export a NAV_WIDTH = 260 constant. These are the single source of truth for the accent.
    Run `npm install` then confirm a build.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm install && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File package.json exists and contains the string "vite"
    - File src/theme/tokens.ts exists and contains "JUSPAY_ACCENT" and "#4F46E5"
    - File tailwind.config.js exists and contains "juspay"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Project installs and builds cleanly; tokens.ts exports JUSPAY_ACCENT = "#4F46E5".</done>
</task>

<task type="auto">
  <name>Task 2: Build reusable primitives + settings modal shell with swapping nav</name>
  <read_first>
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (modal, nav, rows, toggle, dropdown, callout)
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/theme/tokens.ts (accent + nav width)
    - The <visual_contract> block in this plan
  </read_first>
  <files>src/components/primitives/Toggle.tsx, src/components/primitives/Dropdown.tsx, src/components/primitives/SettingRow.tsx, src/components/primitives/Callout.tsx, src/components/settings/navItems.ts, src/components/settings/SettingsNav.tsx, src/components/settings/SettingsContent.tsx, src/components/settings/SettingsModal.tsx, src/App.tsx</files>
  <action>
    Build the primitives exactly per the visual_contract:
    - Toggle.tsx: props { checked: boolean; onChange: (v:boolean)=>void }. Pill w-11 h-6 rounded-full, transition. OFF bg-gray-300, ON uses inline style backgroundColor: JUSPAY_ACCENT (imported from tokens). White knob (size-4) that translates x-5 when on.
    - Dropdown.tsx: props { value: string }. Renders a pill button: <span>{value}</span> + lucide ChevronDown (size 16). text-sm text-gray-700, rounded-full hover:bg-gray-100, px-2 py-1. Static (no menu).
    - SettingRow.tsx: props { label: string; sublabel?: string; control: ReactNode }. Flex row, items-center justify-between, py-4 border-b border-gray-100. Left: label (text-sm font-medium text-gray-900) and optional sublabel (text-xs text-gray-500). Right: {control}.
    - Callout.tsx: props { icon, title, description, actionLabel, onAction?, onDismiss? }. bg-gray-50 rounded-xl p-5, shield icon, bold title, muted description, a bordered rounded-full action button, and a small X (lucide) top-right calling onDismiss.
    navItems.ts: export an ordered array NAV_ITEMS of { id, label, icon } using lucide-react icons, in the exact verbatim order from the visual_contract, with id "payments" for the Payments entry (label "Payments", icon CreditCard).
    SettingsNav.tsx: props { selected: string; onSelect: (id:string)=>void }. Maps NAV_ITEMS to icon+label rows per the contract; active row (id===selected) gets bg-gray-100, others hover:bg-gray-50. Fixed width via NAV_WIDTH. X close button at the top of this column (lucide X) — wire its onClick to a no-op for now (modal is always open in Phase 1).
    SettingsContent.tsx: props { selected: string }. Renders the active section's title heading (text-xl font-semibold) and body: if selected==="general" render <GeneralSection/>; if selected==="payments" render <PaymentsSection/>; otherwise <PlaceholderSection label={...}/>. (These section components are created in Task 3.)
    SettingsModal.tsx: holds useState selected (default "general"). Overlay div fixed inset-0 bg-black/40 flex center; white panel rounded-2xl shadow-xl, max-w ~4xl, min-h ~600px, two columns (SettingsNav | SettingsContent). Pass selected + setSelected down.
    App.tsx: render <SettingsModal/> as the only thing (the modal is the app entry point in Phase 1).
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/components/settings/navItems.ts contains "Payments" and the id "payments"
    - src/components/primitives/Toggle.tsx contains "JUSPAY_ACCENT"
    - src/components/settings/SettingsModal.tsx contains "useState" and "selected"
    - src/components/settings/SettingsContent.tsx contains "PaymentsSection" and "PlaceholderSection"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Modal renders with nav + content; selecting a nav item swaps the content pane; Payments entry exists in the nav.</done>
</task>

<task type="auto">
  <name>Task 3: Section components (General rows, placeholders, Payments stub) + Juspay co-brand</name>
  <read_first>
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/chatgpt screenshots/Screenshot 2026-06-09 at 11.33.31 AM.png (General section rows + MFA callout)
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/components/primitives/SettingRow.tsx
    - /Users/rishi.sharma/Desktop/make/juspay-agent-vault/src/theme/tokens.ts
  </read_first>
  <files>src/sections/GeneralSection.tsx, src/sections/PlaceholderSection.tsx, src/sections/PaymentsSection.tsx, src/components/branding/PoweredByJuspay.tsx</files>
  <action>
    GeneralSection.tsx: recreate the screenshot's General body as static scaffolding using the primitives:
    - A Callout (MFA): shield icon, title "Secure your account", description "Add multi-factor authentication (MFA), like a text message or authenticator app, to help protect your account when logging in.", actionLabel "Set up MFA".
    - SettingRow "Appearance" control=<Dropdown value="System"/>
    - SettingRow "Contrast" control=<Dropdown value="System"/>
    - SettingRow "Accent color" control=<Dropdown value="Default"/>
    - SettingRow "Language" control=<Dropdown value="Auto-detect"/>
    - SettingRow "Enable Dictation" sublabel="Use dictation in the chat composer." control=<Toggle checked onChange/> (default ON, local useState)
    - SettingRow "Spoken language" control=<Dropdown value="Auto-detect"/>
    PlaceholderSection.tsx: props { label }. Renders a muted centered stub: the label + "Settings for this section are not part of this demo." (text-sm text-gray-500). Used by Notifications, Apps, Security, etc.
    PaymentsSection.tsx: STATIC STUB for this plan (wired to the store in Plan 01-03). Render an <h2> "Payments" heading, a one-line intro, a placeholder note "Your cards will appear here.", and the <PoweredByJuspay/> mark at the bottom. Do NOT import any store here — keep it self-contained so this plan has no dependency on the data layer.
    PoweredByJuspay.tsx: a subtle inline mark — small text "Powered by" (text-xs text-gray-400) followed by "Juspay" colored with JUSPAY_ACCENT (inline style color). A tiny rounded dot/badge in the accent color is fine. Keep it understated.
  </action>
  <verify>
    <automated>cd /Users/rishi.sharma/Desktop/make/juspay-agent-vault && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - src/sections/GeneralSection.tsx contains "Appearance" and "Set up MFA"
    - src/sections/PaymentsSection.tsx contains "Payments" and "PoweredByJuspay"
    - src/components/branding/PoweredByJuspay.tsx contains "Powered by" and "JUSPAY_ACCENT"
    - src/sections/PlaceholderSection.tsx contains "not part of this demo"
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>General section mirrors the screenshot rows; non-Payments sections show stubs; Payments shows a static stub with the Powered-by-Juspay mark.</done>
</task>

</tasks>

<verification>
- `npm run dev` opens directly into the settings modal (light theme, rounded white panel on dark overlay).
- Left nav shows all 12 sections including Payments (after Apps).
- Clicking General shows screenshot-like rows (Appearance/Contrast/Accent color/Language/Enable Dictation/Spoken language) + the MFA callout.
- Clicking Payments shows the Payments stub with "Powered by Juspay".
- Clicking any other item shows a placeholder stub.
- `npm run build` exits 0.
</verification>

<success_criteria>
- SHELL-01: settings modal with left nav + content pane in clean light aesthetic ✓
- SHELL-02: nav lists ChatGPT sections + Payments; Payments opens the payments pane ✓
- SHELL-03: "Powered by Juspay" mark + Juspay accent (#4F46E5) present without breaking the ChatGPT look ✓
- SHELL-04: usable at desktop widths ✓
</success_criteria>

<output>
After completion, create `.planning/phases/01-shell-data/01-01-SUMMARY.md` documenting the component tree, the nav item list, the Juspay accent token location, and that PaymentsSection is a static stub to be wired in 01-03.
</output>
