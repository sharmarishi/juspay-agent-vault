---
phase: quick-260609-qx1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/chrome/LandingPage.tsx
  - src/components/chrome/ChatGptBackground.tsx
  - src/components/settings/SettingsModal.tsx
  - src/components/settings/SettingsNav.tsx
  - src/components/settings/SettingsContent.tsx
  - src/App.tsx
autonomous: false
requirements:
  - LAND-01
  - LAND-02
  - SETT-01

must_haves:
  truths:
    - "App opens on the ChatGPT-style landing page; the settings modal is NOT shown by default"
    - "User can click a top-left Settings button (with a Demo tag beside it) to open the settings modal"
    - "User can close the settings modal by clicking the X or clicking the backdrop, returning to the landing page (blurred behind while open)"
    - "On mobile (375px), the sidebar is hidden but a gear icon in the top bar opens settings, and the modal can be closed"
    - "Payments, Shopping, Subagents sections, the mobile nav drawer, and Demo nav tags all still work"
  artifacts:
    - path: "src/components/chrome/LandingPage.tsx"
      provides: "Foreground ChatGPT home with full sidebar, top bar, composer, action chips, and Settings entry point"
      contains: "onOpenSettings"
    - path: "src/components/settings/SettingsModal.tsx"
      provides: "Controlled, closable settings modal"
      contains: "open"
    - path: "src/App.tsx"
      provides: "settingsOpen state (default false) wiring landing + modal"
      contains: "settingsOpen"
  key_links:
    - from: "src/App.tsx"
      to: "src/components/chrome/LandingPage.tsx"
      via: "onOpenSettings prop → setSettingsOpen(true)"
      pattern: "onOpenSettings"
    - from: "src/App.tsx"
      to: "src/components/settings/SettingsModal.tsx"
      via: "open + onClose props"
      pattern: "open=\\{settingsOpen\\}"
    - from: "src/components/settings/SettingsNav.tsx"
      to: "onClose"
      via: "desktop X button onClick"
      pattern: "onClick=\\{onClose\\}"
---

<objective>
Convert the app from an always-open settings modal floating over a static blurred
background into a landing-first experience: a real, foreground ChatGPT-style home
page is the default view, and the settings modal opens only when the user clicks a
Settings entry point (top-left on desktop with a "Demo" tag; a gear icon in the top
bar on mobile).

Purpose: Give the demo a believable ChatGPT home and a deliberate, discoverable path
into the Payments/Shopping demo — instead of the modal being permanently mounted.
Output: A foreground `LandingPage`, a controlled+closable `SettingsModal`, and `App.tsx`
owning `settingsOpen` state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md

@src/App.tsx
@src/components/chrome/ChatGptBackground.tsx
@src/components/settings/SettingsModal.tsx
@src/components/settings/SettingsNav.tsx
@src/components/settings/SettingsContent.tsx
@src/theme/tokens.ts

<interfaces>
<!-- Current contracts the executor must update. Extracted from codebase. -->

Current SettingsModal (self-managing, NOT controlled — must change):
```tsx
export function SettingsModal() { ... }   // no props; always rendered; X is a no-op
```

Target SettingsModal contract:
```tsx
interface SettingsModalProps { open: boolean; onClose: () => void }
export function SettingsModal({ open, onClose }: SettingsModalProps)  // render null when !open
```

Current SettingsNav props (keep, add onClose wiring to desktop X):
```tsx
interface SettingsNavProps {
  selected: string;
  onSelect: (id: string) => void;
  open: boolean;        // mobile drawer state
  onClose: () => void;  // currently only closes mobile drawer
}
// Desktop X button currently: onClick={() => {}}  ← wire to a CLOSE-MODAL handler
```
NOTE: SettingsNav.onClose closes the mobile DRAWER. The desktop X must close the
MODAL. Do NOT reuse the drawer onClose for the modal X — add a separate
`onCloseModal` prop (or pass the modal close down explicitly). Be careful not to
conflate the two.

Current SettingsContent props (keep, add modal-close affordance):
```tsx
interface SettingsContentProps {
  selected: string;
  onOpenNav: () => void;  // opens mobile drawer (hamburger)
}
// Mobile header has hamburger (Menu) + title. Add an X on the RIGHT that closes the MODAL.
```

Demo tag pill style (reuse exactly — already used for nav tags):
```
bg-indigo-50 text-indigo-600 border border-indigo-100
```

Tokens: JUSPAY_ACCENT = "#4F46E5", JUSPAY_NAME = "Juspay"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the foreground LandingPage (rename + expand ChatGptBackground, add Settings entry point)</name>
  <files>src/components/chrome/LandingPage.tsx, src/components/chrome/ChatGptBackground.tsx</files>
  <action>
Rename `ChatGptBackground.tsx` → `LandingPage.tsx` and export `function LandingPage({ onOpenSettings }: { onOpenSettings: () => void })`. Keep the `OpenAILogo` SVG helper. This is a foreground page now, not a backdrop — it still fills the viewport (`fixed inset-0` is fine since it is the base layer) with `bg-[#f9f9f9] text-gray-700 overflow-hidden`. It is a non-interactive demo recreation EXCEPT the Settings entry points.

Expand from the current minimal rail into the full home layout (match the feature spec / screenshot):

- LEFT SIDEBAR (desktop only, `hidden sm:flex flex-col w-[260px]` with right border): 
  - TOP: a row with the OpenAI logo + a sidebar-collapse icon (`PanelLeft`).
  - **Settings entry point (TOP-LEFT requirement):** directly below/near the logo at the top of the sidebar, a clearly visible button: gear icon (`Settings` from lucide) + "Settings" label, with a "Demo" pill beside it using the exact nav-tag style `bg-indigo-50 text-indigo-600 border border-indigo-100` (small `text-[10px] font-medium rounded-full px-1.5 py-0.5`). `onClick={onOpenSettings}`. This is the KEY interactive element — make it obviously clickable (hover bg).
  - Primary nav items (icons + labels, non-interactive): "New chat" (highlighted with `bg-gray-100`, use `SquarePen`), "Search chats" (`Search`), "Library" (`Image`/`BookOpen` — pick a sensible lucide icon), "Projects" (`FolderClosed`), "Apps" (`Grid3x3`), "Codex" (`Code2`), "More" (`MoreHorizontal`).
  - "Recents" heading (`text-xs text-gray-400 uppercase` style) with sample chat titles: "Hotel booking in Mysore", "Payment setup prompt", "Pay bill assistance", "Fintech Terms Explained", "Initial greeting".
  - Pinned at BOTTOM (`mt-auto`): a user profile row — small round avatar (`h-8 w-8 rounded-full bg-gray-300`), "Rishi Sharma" / "Free" (two lines, name + plan) — and a "Claim offer" button.
- TOP BAR: left = "ChatGPT ⌄" (`ChevronDown`). On MOBILE only, left side ALSO shows a gear icon button (`Settings`) that calls `onOpenSettings` (plus a "Demo" pill beside it if it fits) — this is the mobile entry into the demo since the sidebar is hidden (`sm:hidden`). Right = "Free offer" (`Gift`, indigo) + a small placeholder icon (keep the existing dashed-border square).
- CENTER (`main`): heading `What's on your mind today?` (e.g. `text-2xl/text-3xl font-medium text-gray-800`, centered) above the existing composer pill ("Ask anything", `Plus` icon, `SlidersHorizontal` + dark circular `AudioLines` voice button). BELOW the composer, three action chips in a row (wrap on mobile): "Create an image" (`Image`), "Write or edit" (`Pencil`), "Look something up" (`Search`) — each a rounded pill `border border-gray-200 bg-white px-3 py-1.5 text-sm` with a small lucide icon.

Responsive: sidebar `hidden sm:flex`; on mobile the center heading + composer remain centered and usable; chips wrap. Keep the clean ChatGPT-neutral aesthetic — only the Demo pill uses indigo.

Only import lucide icons that exist (lucide-react ^0.469); verify icon names compile. No new dependencies. Keep all text/chrome non-interactive except the two Settings entry points.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`src/components/chrome/LandingPage.tsx` exists exporting `LandingPage({ onOpenSettings })`; `ChatGptBackground.tsx` is removed/replaced; build (tsc + vite) is clean; sidebar Settings+Demo button and mobile gear both call `onOpenSettings`.</done>
</task>

<task type="auto">
  <name>Task 2: Make SettingsModal controlled + closable (props open/onClose, backdrop + desktop X + mobile X)</name>
  <files>src/components/settings/SettingsModal.tsx, src/components/settings/SettingsNav.tsx, src/components/settings/SettingsContent.tsx</files>
  <action>
**SettingsModal.tsx:** Add props `{ open: boolean; onClose: () => void }`. Return `null` when `!open`. Keep internal `selected`/`navOpen` state and `handleSelect`. 
- The outer backdrop div (`fixed inset-0 bg-black/40 backdrop-blur-sm ...`) gets `onClick={onClose}` so clicking outside closes. Add `onClick={(e) => e.stopPropagation()}` to the white panel div so clicks inside don't bubble to the backdrop. (The mobile drawer in SettingsNav uses `z-[70]` above the panel — its own scrim already calls the drawer onClose, so it is unaffected.)
- Pass the modal-close handler down: add `onCloseModal={onClose}` to `<SettingsNav>` and `onClose={onClose}` to `<SettingsContent>` (a NEW prop on SettingsContent for the modal close — distinct from `onOpenNav`).
- Keep `navOpen`/`onClose={() => setNavOpen(false)}` on SettingsNav for the mobile DRAWER (do not overwrite it).

**SettingsNav.tsx:** Add a new prop `onCloseModal: () => void` (separate from the existing `onClose` which closes the mobile drawer). Wire the DESKTOP sidebar X button: change `onClick={() => {}}` → `onClick={onCloseModal}`. Leave the mobile drawer X (`onClick={onClose}`) and scrim as-is.

**SettingsContent.tsx:** Add a new prop `onClose: () => void` (closes the MODAL). In the mobile header row (which has the hamburger `Menu` + title), add an X button (`X` from lucide) on the RIGHT (`ml-auto`, `md:hidden`) wired to `onClose` so mobile users can close the near-full-screen modal. Keep the existing hamburger (`onOpenNav`) and title. Use the same button styling as the existing icon buttons (`p-1.5 rounded-lg text-gray-600 hover:bg-gray-100`).

Do not touch the section rendering (General/Payments/Shopping/Placeholder) or the desktop layout dimensions. Be careful: SettingsNav now has BOTH `onClose` (drawer) and `onCloseModal` (modal) — keep them distinct.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>`SettingsModal` accepts `{ open, onClose }`, renders null when closed, closes on backdrop click (panel stops propagation); desktop sidebar X and mobile header X both call `onClose`; mobile drawer hamburger/X still work; build clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire App.tsx — landing-first with settingsOpen state (default closed)</name>
  <files>src/App.tsx</files>
  <action>
Replace the always-rendered modal model. Import `LandingPage` (from `./components/chrome/LandingPage`) instead of `ChatGptBackground`, and `SettingsModal`. Add `const [settingsOpen, setSettingsOpen] = useState(false);`.

Render:
```tsx
<>
  <LandingPage onOpenSettings={() => setSettingsOpen(true)} />
  <SettingsModal open={settingsOpen} onClose={() => setSettingsOpen(false)} />
</>
```
Default is FALSE → app lands on the home page; the modal mounts on top (with its own backdrop-blur dimming the landing behind it) only when opened. Remove the stale `ChatGptBackground` import.
  </action>
  <verify>
    <automated>npm run build && npm test</automated>
  </verify>
  <done>App renders LandingPage by default with no visible modal; clicking Settings opens it; build clean and all existing tests (24) stay green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verify landing-first flow + open/close + responsive</name>
  <what-built>Landing-first app: foreground ChatGPT-style LandingPage is the default view; a controlled, closable SettingsModal opens from a top-left Settings button (with Demo tag) on desktop and a gear icon in the top bar on mobile.</what-built>
  <how-to-verify>
Run `npm run dev` and open the app.

Desktop (full width):
1. App lands on the ChatGPT-style home — NO settings modal visible by default.
2. Top-left of the sidebar shows a "Settings" button (gear) with a subtle indigo "Demo" pill beside it.
3. Click Settings → the settings modal opens over the (blurred) landing page.
4. Close it via the desktop X (top of the settings sidebar) → returns to landing.
5. Open again, then click the dark backdrop OUTSIDE the white panel → it closes. Clicking INSIDE the panel does NOT close it.
6. Inside the modal, confirm Payments, Shopping, and Subagents content still work, and the Demo nav tags still show.

Mobile (resize to 375px):
7. Sidebar is hidden; a gear icon appears in the top bar (left) — tap it → settings opens (near full-screen).
8. The mobile nav drawer (hamburger) still slides in from the left and lets you pick sections.
9. Close the modal via the X in the mobile header → returns to the landing page.
10. Landing heading + composer remain centered/usable; action chips wrap nicely.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues (layout, missing entry point, broken close, regressions).</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` (tsc + vite) completes with no type or build errors.
- `npm test` — existing 24 tests stay green (no test files were modified).
- LandingPage exports `onOpenSettings`; SettingsModal accepts `open`/`onClose` and renders null when closed.
- No regressions in Payments/Shopping/Subagents sections, the mobile nav drawer, or Demo nav tags.
</verification>

<success_criteria>
- App opens on the landing page by default (modal closed).
- Desktop top-left Settings button + Demo tag opens the modal; closes via X and backdrop.
- Mobile gear icon opens the modal; X in mobile header closes it; nav drawer preserved.
- Build clean, tests green, no section regressions.
</success_criteria>

<output>
After completion, create `.planning/quick/260609-qx1-add-a-chatgpt-landing-page-home-as-the-d/260609-qx1-SUMMARY.md`
</output>
