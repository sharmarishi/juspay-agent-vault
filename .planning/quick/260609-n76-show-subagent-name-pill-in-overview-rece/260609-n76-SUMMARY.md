---
quick_id: 260609-n76
status: complete
date: 2026-06-09
---

# Quick Task 260609-n76 — Summary

## Bug
Overview → "Recent transactions" showed only the merchant name, not the subagent —
unlike the Transactions tab. (DashboardOverview was deliberately left untouched in
the Subagents task since it only referenced `t.merchant`.)

## Fix
`src/components/payments/DashboardOverview.tsx`:
- Added the `subagents` store selector and a `subagentName(subagentId)` helper.
- Added the accent-tinted subagent pill next to the merchant in each Recent
  transactions row, mirroring the `TransactionsList` pattern exactly
  (`color: JUSPAY_ACCENT`, `backgroundColor: JUSPAY_ACCENT + "14"`).

## Verification
- `npm run build` (tsc + vite) — clean.
- `npm test` — 24/24 pass.

## Notes
Executed inline (no subagents) — small UI parity fix. GSD artifacts + atomic commit
+ STATE tracking preserved.

## Human-verify
Overview → Recent transactions → each row now shows merchant + the colored subagent
pill, matching the Transactions tab.
