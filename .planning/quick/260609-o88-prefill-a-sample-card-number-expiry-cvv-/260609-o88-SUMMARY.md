---
quick_id: 260609-o88
status: complete
date: 2026-06-09
---

# Quick Task 260609-o88 — Summary

## Goal
Pre-fill a sample card (number, expiry, CVV) in the Add Card "Card" form so adding a
physical card is effectively one click.

## What changed
`src/components/cards/AddCardModal.tsx`:
- Added sample constants: `SAMPLE_CARD_NUMBER = "4242 4242 4242 4242"`,
  `SAMPLE_CARD_EXPIRY = "12/27"`, `SAMPLE_CARD_CVV = "123"`,
  `SAMPLE_CARD_HOLDER = "Rishi Sharma"`.
- Physical form state now initializes to those samples, and `resetForm` restores them
  (so the sample stays populated when the modal reopens).
- **Added a CVV field** (it didn't exist before): the form now has Card number (full),
  a 2-col Expiry | CVV row, then Cardholder name (full). CVV is form-only — not persisted,
  consistent with the existing "only the last 4 digits are stored" mock note.

## Verification
- `npm run build` (tsc + vite) — clean.
- `npm test` — 24/24 pass.

## Notes
Executed inline (no subagents) — small form change. GSD artifacts + atomic commit +
STATE tracking preserved.

## Human-verify
Add card → "Card" tab → number/expiry/CVV/holder are pre-filled; clicking "Add card"
works immediately.
