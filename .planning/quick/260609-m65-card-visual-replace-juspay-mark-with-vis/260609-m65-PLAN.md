---
quick_id: 260609-m65
mode: quick
status: complete
date: 2026-06-09
---

# Quick Task 260609-m65: Visa mark on card + terminology rename

## Goal

1. Replace the small "Juspay" wordmark at the bottom-right of the card visual with
   the **Visa logo** (asset provided in project folder).
2. Rename user-facing terminology everywhere: **"Physical Card" → "card"**,
   **"Virtual Card" → "tokens"** (singular "token" for a single item).

## Scope / guardrails

User-facing **display strings only**. The `CardType` union, the `"physical"`/
`"virtual"` literal values, variable names, code comments, and test names are
internal and were intentionally NOT changed (renaming them would break the data
model, seed, persistence migration, and tests).

## Tasks

### Task 1 — Visa logo on card visual
- Relocate `visa logo.svg.png` (repo root, untracked) → `src/assets/visa-logo.png`.
- `CardVisual.tsx`: drop `JUSPAY_NAME` import, add `import visaLogo from "../../assets/visa-logo.png"`.
- Replace the Juspay `<span>` with the Visa `<img>` inside a small white rounded
  chip (`bg-white rounded px-1.5 py-1`, `h-3` logo) — the asset is the blue Visa
  wordmark, so a white backing keeps it legible on every card gradient color and
  reads like a real card. `alt="Visa"`.

### Task 2 — Terminology rename (display strings)
- `CardVisual.tsx` type badge: `"Physical" / "Virtual"` → `"Card" / "Token"`.
- `AddCardModal.tsx`: segment `"Physical card" → "Card"`, `"Custom virtual" → "Custom token"`;
  submit `"Add physical card" → "Add card"`; template blurb `"generate a virtual card" → "generate a token"`;
  selector label `"Issued under (physical card)" → "Issued under (card)"` (×2);
  warning `"Add a physical card first before creating a virtual card." → "Add a card first before creating a token."` (×2).
- `CardDetailModal.tsx`: tab label + heading `"Virtual cards" → "Tokens"`;
  empty state `"No virtual cards yet…" → "No tokens yet…"`.
- `PaymentsSection.tsx` empty state already reads "No cards yet." — no change.

**Verify:** `npm run build` (tsc + vite) clean; `npm test` 18/18.
**Done:** Card shows Visa mark; no user-facing "Physical Card"/"Virtual Card" text remains.

## Human-verify
Open a card — confirm the Visa logo at bottom-right is legible across card colors,
the type badge reads CARD/TOKEN, and Add-card / detail tabs use "card"/"Tokens".
